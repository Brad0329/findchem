# FindChem — 물질명·CAS로 유해화학물질 규정수량(고시 별표 2·3) 조회

Flutter(Android + 웹 한 벌), 서버 없음, 데이터는 JSON으로 번들해 처음부터 만드는 신규 시스템.

## 현재 단계
- 전체 Phase 체크리스트/아키텍처의 **단일 원본 = `work_log/plan.md`** (여기에 중복 기재 금지)
- 현재: **F-008 규정수량·최대보유량 판정 도우미** — **1단계(자산화 `lib/assist/`) 완료·master 병합 2026-09-16, 앱 모델 Opus 5 확정.
  다음은 2단계(앱·웹 화면 + LLM 루프, 모델 선택 설정 포함)** — 상세는 plan.md F-008 항목·REQUIREMENTS F-008 /
  **F-007**(웹 단계 완료·배포, GHS 그림문자 표·H·P 문구 2026-09-15 완료, 앱 단계는 결정 대기) → 그 뒤
  **Phase 007 — 부채 정리** (미착수. 항목은 plan.md의 Phase 007 절 — `/debt-audit` 2026-09-12 결과.
  직전에 끼워 넣은 F-005 자주보는 Chem 목록은 2026-09-13 완료). (완료 시 이 줄과 plan.md 체크박스를 함께 갱신)
- 배포처: APK는 직접 설치(릴리스 키 필요 — `팩_Flutter_Android.md`), 웹은 master push → Actions →
  https://brad0329.github.io/findchem/

## 새 세션 시작 시
1. `work_log/plan.md` — 전체 현황·아키텍처·완료 Phase
2. 진행 중 Phase의 `work_log/Phase_XXX.md`(있으면) — 이전 결정과 실패 경험을 이어받는다

## 요구사항 정책 ★"정답 기준"이 없으면 완료를 판단할 수 없다
- 기능 요구사항 + 수용 기준의 **단일 원본 = `docs/REQUIREMENTS.md`**
- **구현 전**: 수용 기준이 먼저 있어야 한다 — 없으면 스펙화하고 모호점은 사용자에게 질문.
  **화면을 그리는 첫 기능 전에는 REQUIREMENTS의 '화면 방향' 절도** 채워져 있어야 한다 — 비어 있으면
  그 자리에서 묻는다(사용자는 잊는 것이 정상, 기억하는 쪽은 AI). 안 고르면 AI 기본값이 곧 디자인이 된다.
- **요구사항이 바뀌면 즉시 갱신** — 대화 속 합의로만 남기면 다음 세션이 구버전으로 구현한다.
- **수용 기준은 검증 가능한 문장으로**("X를 넣으면 Y가 N건") — 그대로 테스트 케이스가 된다.
- **YAGNI**: 문서에 없는 요구사항을 위한 추상화·설정화·일반화 금지.
- 한 번에 훑기 어려워지면(대략 기능 15~20개) 도메인별로 분할하고 REQUIREMENTS.md는 목차+링크 인덱스로.
- **상태 어휘를 구분한다: 미착수/진행/완료/동결/중단/폐기/안 함.** 보류와 "안 할 것"을 섞지
  않고, 닫은 결정에는 날짜·근거를 남긴다(조사 기록은 지우지 않는다). 상세: `노하우_프로세스.md`
- **요구사항 ID 대장 — 새 번호는 여기서 받아간다.** 아이디어 단계여도 번호를 잡았으면 적는다.
  **번호를 재사용하지 말 것.** 발급 시 plan.md 보류 항목의 후보 번호까지 훑는다(`노하우_프로세스.md`).
  새 번호는 **새 기능에만** — 앞 기능의 마무리·문구는 앞 절 한 줄 수정, 작은 기능은 한 줄 형태(F-002 예시).

  | ID | 기능 | 상태 |
  |---|---|---|
  | F-001 | 물질 검색 | 완료(2026-09-11) |
  | F-002 | 원천자료 update (PDF 업로드 → 앱 내 파싱) | 완료(2026-09-12) |
  | F-003 | 카드 공유 (평문, 공유 창 없으면 클립보드) | 완료(2026-09-11) |
  | F-004 | 웹 표 화면 + 행 복사(TSV) — 앱은 카드 유지 | 완료(2026-09-12) |
  | F-005 | 자주보는 Chem 목록 (카드 저장 — 앱만) | 완료(2026-09-13) |
  | F-006 | APK 받기 페이지(GitHub Releases) + 앱 버전 표시 | 진행 |
  | F-007 | CAS 부가 정보 조회(공공데이터 API 3종, 키는 설정에서) + 설정 카드 구성 | 진행(웹 완료 2026-09-14, 앱 단계 결정 대기) |
  | F-008 | 규정수량·최대보유량 판정 도우미 — 앱·웹에서 질문하면 LLM이 검색 도구·규칙 원문을 불러 답 | 진행(1단계 완료 2026-09-16·앱 모델 Opus 5 확정, 2단계 미착수) |
  | F-009 | 최대보유량 산정 보조(비중 데이터 + 산정 입력폼) — **착수 전 비중 출처 조사가 선행** | 미착수(2026-09-16 번호 발급) |
  | **다음 번호** | **F-010** | |

## 데이터 모델/스키마 변경 게이트 ★스키마는 되돌리기 비싸다
- 스키마 **결정의 기록 = `docs/SCHEMA.md`** (설계 의도, 관계, 제약, 변경 이력).
- **AI는 임의로 컬럼 추가/변경, 마이그레이션 수정을 하지 않는다.**
  ① 변경안(무엇을·왜·영향 범위)을 SCHEMA.md에 먼저 기록 → ② 사용자 확인 → ③ 반영.
- **SCHEMA.md는 "결정과 이유"를, 코드는 "현재 상태"를 담당한다.** 둘이 어긋나면 코드가 정답이고
  SCHEMA.md를 고친다. 컬럼 목록을 손으로 이중 관리하지 말 것.
- **사용자 데이터와 빌드 산출물을 같은 파일·DB에 두지 않는다** — 산출물은 재빌드 때 통째로 교체되어
  그 안의 사용자 데이터가 지워진다. 저장·캐시는 별도 파일로.
- **누적되는 저장소의 스키마 변경에는 마이그레이션을 같은 커밋에** — `CREATE TABLE IF NOT
  EXISTS`는 기존 테이블을 고치지 않는다(실사례). 상세: `노하우_설계.md`

## 보안 3층 ★매 커밋이 아니라 "무엇을 바꿨나"로 걸린다 (2026-09-09 사용자 결정)
- **① 코딩 규칙 — 항상, 별도 단계 없음**: 질의·셸·파일 경로에 사용자 입력을 문자열로 조합하지 않는다
  (바인딩·ORM·경로 정규화) / 인증·권한은 fail-closed — 설정이 없으면 기동 거부 / 오류 응답에는 상태만,
  원인·경로·사용자명은 로그에만 / 비밀정보는 '불변 규칙'대로 `.env`. 확인은 사다리의 정적 분석에 묻어간다.
  **"입력이 DB에 닿는" 보통 기능은 여기서 끝난다** — 초기 개발의 거의 모든 기능이라 점검을 붙이면
  사다리가 무너진다. 목록 실물: `노하우_설계.md` 보안 기본값.
- **② 범위 점검 — 닿을 때만**: 인증·세션·권한 판정 / 파일 업로드·외부 실행물 / 결제 / 비밀정보 취급 /
  새 공개 표면(엔드포인트·웹훅·폼)을 넣거나 고치는 커밋은 **그 변경 범위만** 점검한다(`/security-review`,
  못 쓰면 `노하우_설계.md`의 공개 엔드포인트 점검 목록으로 직접) — 결과는 커밋 메시지에. 화면·문구·내부
  로직에는 붙이지 않는다. **게이트: 첫 외부 입력 진입점을 만들기 전에 REQUIREMENTS '비기능 요구사항'의
  인증 방식·공개 범위**가 채워져 있어야 한다 — 비어 있으면 그 자리에서 묻는다('화면 방향'과 같은 원리,
  안 고르면 AI 기본값이 곧 보안 정책이 된다).
- **③ 전체 리뷰 — 공개 직전 1회**: 공개·배포 준비 Phase에서 `/security-review` 전체(원격 없으면 직접).
  **통과가 공개의 선행 조건** — 조합돼야 드러나는 결함(권한 우회 경로·세션)은 ②로는 안 잡힌다.

## 구조
- **디렉토리 뼈대 (파일 생성 위치의 최종 기준 — 2026-09-11 확정, Flutter 프로젝트 = 저장소 루트)**:
  ```
  lib/                 ← 앱 소스(Dart)
  test/                ← Dart 테스트 (flutter test 기본 위치)
  tests/               ← 동봉 파이썬 도구(훅·측정기) 테스트
  assets/              ← 원천 PDF 3개·유해성 분류·HCODE·PCODE xlsx(번들 안 함) + data/(번들 JSON, data/ghs/ = GHS 그림문자 — pubspec에는 이 두 폴더만 등록)
  android/  web/       ← 플랫폼 (flutter create 생성물. 아이콘은 scripts/make_icons.py가 굽는다. web/download/ = APK 받기 페이지 F-006)
  docs/                ← REQUIREMENTS.md, SCHEMA.md, design/, playbooks/
  work_log/            ← plan.md, Phase_XXX.md
  scripts/             ← 빌드·유틸 스크립트
  .github/workflows/   ← GitHub Actions (웹을 GitHub Pages에 배포 — Phase 006)
  ```
  루트의 `pubspec.yaml`·`pubspec.lock`·`analysis_options.yaml`·`.metadata`는 Flutter 고정 파일이다.
  **이 트리 밖에 새 최상위 디렉토리/파일을 만들지 말 것.** 필요하면 사용자 확인 후 이 목록부터 갱신.
- 설정/환경: 환경별 설정이 없다 — 앱에 서버가 없다. `.env`는 개발 PC 조사용 키뿐이고 앱은 읽지 않는다.
  **예외(F-007 기본 키, 2026-09-14 사용자 결정 — F-008 Anthropic 키도 같은 방식, 2026-09-16)**: 웹 배포 빌드가 Actions Secret
  `DATA_GO_KR_KEY`를 `--dart-define`으로 넣는다 — 배포된 JS에서 공개됨을 감수했다(REQUIREMENTS 비기능 4). 키를 저장소 파일로 두지 않는 규칙은 그대로다.
- **클라우드 세션(claude.ai/code) 환경의 원본 = `scripts/cloud_setup.sh`** — 웹 화면 Setup script 칸은 그 사본이라
  고치면 다시 붙여 넣는다. 클라우드는 analyze·test·웹 빌드까지(Android SDK 없음), `settings.local.json`은 가지 않는다.
- 새 기능 추가 위치 규칙: `lib/` 아래 도메인별 폴더(파서·검색·화면 등, 착수하는 Phase에서 만든다).
  `main.dart`에 로직을 쌓지 않는다. 테스트는 `test/`에 같은 폴더 구조로.
- **같은 규칙이 두 곳 이상에 구현되면**(언어가 달라 이식이 불가피한 경우 등) 그 사실을 여기 적고,
  **두 구현을 대조하는 테스트를 반드시 만든다.** 실물: PDF 추출 층(`lib/parser/pdf_extractor.dart`) ↔ pdfplumber
  정답지(`scripts/oracle_pdf_cells.py` → `test/fixtures/`, 규칙 없는 원시 셀) — 대조는 `test/parser/pdf_extractor_test.dart`.
  같은 기계로 **손으로 옮긴 규칙 원문**(`lib/assist/rules.dart`) ↔ PDF 표 밖 문단(`scripts/oracle_rule_text.py` →
  `test/fixtures/rule_text.json`) — 대조는 `test/assist/rules_test.dart`.

## 불변 규칙
- **Silent failure 절대 금지**: 모든 예외 상황을 명시적으로 로깅한다. 빈 except/catch 금지.
  넘어가야 할 정당한 이유가 있으면 그 이유를 로그+주석으로 남긴다.
- **조용한 절단 금지**: 결과를 상한으로 자르면 **잘랐다는 사실과 전체 건수를 함께** 돌려준다.
  페이지네이션 첫 페이지만 받기·라이브러리 기본 limit·로그 꼬리처럼 **암묵적으로 잘리는 것도 절단이다.**
  목록·검색 기능의 수용 기준에 "N건을 넘으면 전체 건수와 '더 있음'을 표시"를 넣어 테스트로 건다.
- **손으로 만든 데이터에는 자동 검증을 붙인다**: 사람이 쓴 매핑·목록은 반드시 틀린다.
- **허용의 기준은 "위험"이다 — 파괴적이지 않으면 연다** (2026-08-22 사용자 결정, 종전 "허용 목록을
  넓히지 않는다" 대체. 1인·서버 없음 전제 — 협업·운영 서버가 생기면 다시 묻는다).
  **루틴 작업(커밋·테스트·빌드·설치·조사)에 승인 창이 뜨면 그것이 결함이다.** 파괴적 명령은 deny,
  받은 실행물 실행·패키지 **추가**(잠금 파일에 없는 새 의존성)는 건별 확인 — 잠금 파일대로 설치하는
  `pip install -r`·`pub get`·`npm ci`는 추가가 아니라 설치이므로 연다. 반복되면 ① 허용된 형태로 부른다(습관 — 반복되면 훅)
  ② 규칙 추가. 점검은 Phase 종료 시 `/approval-audit`. 대가·목록 실물: `노하우_승인_대기_최소화.md`
- **비밀정보(API 키, 접속정보)는 이 파일·커밋·채팅에 절대 쓰지 않는다.** `.env`에 두고 gitignore한다.
- `<프로젝트 진행하며 확정되는 규칙을 여기 추가>`

## 검증 원칙
- **"됐다"는 실제 결과로 판단한다.** 빌드 성공·프로세스 살아있음은 근거가 아니다.
  화면을 보거나, 실제 응답을 확인하거나, 데이터를 직접 질의한다.
- **기능 완료 시**: REQUIREMENTS.md의 수용 기준을 하나씩 짚어 대조하고 → **그 자리에서 REQUIREMENTS의
  상태와 plan.md를 둘 다 갱신한다.** 코드가 맞아도 정답지가 틀리면 다음 세션이 오독한다.
- **Phase 완료 시**: 전체 테스트 통과가 다음 Phase의 선행 조건. plan.md 체크·커밋·push까지 끝나면
  **"새 Phase는 새 세션을 열어 진행하기를 권고합니다"라고 말하고 멈춘다** — 다음 Phase를 이 세션에서
  시작하지 않는다. 이어받기는 plan.md와 Phase 로그가 한다(2026-09-09 사용자 결정).
- **새로 넣거나 고친 테스트는 실제로 무언가를 잡는지 확인한다** — 대상 코드를 일부러 틀리게 만들어
  실패를 한 번 본다(변이가 실제로 적용됐는지까지 확인, 한 번에 하나씩). "넘치지 않는다·깨지지 않는다"류는
  실제 값(폭·크기·건수)을 재야 잡는다 — 존재 확인(find류)은 잘려도 통과한다.
- **자동 테스트/코드 리뷰/사람 실테스트는 잡는 결함의 층이 다르다 — 서로 대체 불가.**
  실테스트에는 "고장 상태를 만들어야 드러나는" 시나리오를 명시적으로 요청한다.
- **표본 2건은 실측이 아니다** — 기록에 표본 수·확인 파라미터를 함께 적는다.
- 자동화 불가 항목(실기기·실결제)은 plan.md "사용자 실테스트 대기"에 — **확인 전 완료 선언 금지.**
- 상세(변이 확인법, 검증의 층, 디버깅 원칙): `docs/playbooks/노하우_검증.md`

## 검증 게이트 ★도구가 아니라 목적을 지킨다
- **게이트 4종은 도구가 없어도 건너뛰지 않는다** — 조사→`researcher`, 스펙화→`requirements-analyst`,
  수용 기준 대조→`spec-checker`, Phase 통합 테스트→`qa-tester`(foreground). 에이전트를 못 쓰면
  메인 agent가 **같은 원칙·같은 보고 형식으로 직접** 하고 그 사실을 커밋 메시지에 적는다
  (원칙은 각 `.claude/agents/*.md`에 있다).
- **사용자 호출 스킬**: `/code-review`(못 하면 커밋 메시지에 남기고 넘어간다) · `/security-review`
  (시점은 '보안 3층' ②·③, `origin` 원격 필요, 없으면 직접 점검) · `/debt-audit`(**Phase 3~5개마다 사용자에게 시점을 알린다** —
  목록만 만들고 정리 여부는 사용자 결정).
- **넓은 조사는 서브에이전트 병렬로**(코드 탐색 `Explore`·`general-purpose`, 데이터·API 조사
  `researcher`) — 메인은 **결론만** 보관한다. PLAN MODE는 plan.md 최초 작성·스코프 변경 시.

## 작업 속도 규칙 ★실제로 샌 시간을 재고 정한다
- **느리다고 느껴지면 테스트가 아니라 승인 대기부터 잰다**(실측: 테스트 30초 vs 대기 6분).
  도구: `scripts/measure_wait.py`(벽시계 실측)·`measure_approvals.py`(원인 분류, 예측).
  단 한 메시지의 호출은 차례로 돈다 — 짧은 Write는 긴 명령(analyze·test·build)보다 앞에(`↳ 겹침` 줄, 2026-09-15).
  처방: `docs/playbooks/노하우_승인_대기_최소화.md`
- **★ 검증 강도는 트랙이 아니라 "무엇을 바꿨나"로 정한다** (2026-09-05 사용자 결정 — 주석 한 줄에도
  변이 확인이 붙던 것을 없앤다):
  문서·주석·문구(동작 불변) = diff 읽기, 코드 파일이면 정적 분석 1회, 테스트 0 /
  화면·설정·배치(동작 변경, 로직 아님) = 정적 분석 1 + 관련 테스트 1(관련 = 바뀐 파일을 직접 쓰는 테스트) /
  로직 변경 = 정적 분석 1 + 관련 테스트 1 / **변이 확인은 테스트를 새로 넣거나 고쳤을 때만**('검증 원칙') —
  변이는 코드가 아니라 테스트를 검증하는 것이라 테스트를 안 건드린 한 줄 수정에는 붙지 않는다 /
  커밋 직전 전체 실행 = 로직이 바뀐 커밋만. 생략에는 근거(의존 그래프). 외부 API 실측·문서 갱신은 줄이지 않는다.
  **일반 트랙(데이터·기본 구조)은 종류가 다른 검증을 탄다** (2026-09-06): 전체 테스트 + **구버전 저장본을
  실제로 읽어 마이그레이션 실측**(새 코드로 만든 빈 DB는 통과하고 사용자가 쌓은 옛 DB만 깨진 실사례 —
  `노하우_설계.md`) + 되돌리기 경로(백업·롤백) 확인 + 다중 구현이면 대조 테스트 존재 확인 +
  **배포 전 사용자 실테스트 필수.**
- **★ 긴 명령이 곧 긴 승인 대기다**(38초 vs 83초 실측). 웹 페이지는 curl이 아니라 `WebFetch`로 /
  `python -c` 인라인 금지(훅) / 세션 UUID가 든 스크래치패드 절대경로·한글 인자를 셸 명령에 쓰지 않는다 /
  **일회성 명령은 "항상 허용"으로 열지 않는다** — 다시 안 걸리는 죽은 규칙만 남는다.
- **커밋은 작업 단위마다.** 검증 결과(테스트 범위·수용 기준 대조·**대체 수단을 썼으면 그 사실과 이유**)는
  Phase 로그가 아니라 **커밋 메시지에** 적는다 — 조용히 건너뛰지 않는다.

## 작업 트랙 ★저위험이 기본값이다 — 기준은 "몇 줄이냐"가 아니라 "되돌리기가 비싼가"

전제: 1인 프로젝트(협업 시 재정의). 병목은 왕복 횟수다. 역사: `docs/playbooks/노하우_작업_트랙.md`

- **일반 트랙은 ① 데이터 ② 기본 구조에 닿을 때만** — 그 외 전부(화면·문구·설정·배치, 새 기능 포함)는
  저위험. ① 스키마, 빌드 파이프라인·보정 데이터, 기존 저장을 못 읽게 되는 저장 형식 변경
  (저장 파일을 하나 **더 늘리는 것**은 제외) ② 다중 구현(parity) 묶음, 동결 경계, 디렉토리 뼈대,
  화면·모듈 전환 구조. 애매하면 저위험으로 시작하고, 닿으면 멈추고 **사용자에게 묻는다** —
  혼자 일반 트랙으로 올리지 않는다. 검증은 사다리의 **일반 트랙 갈래**(구버전 데이터 실측·되돌리기 경로).
  이 프로젝트의 실물(2026-09-11): ① 번들 JSON 형식(SCHEMA.md), PDF 파서(번들 JSON과 F-002 저장본을 둘 다
  만드는 파이프라인), 기기 저장본 형식 ② 디렉토리 뼈대, 파서 공유 경계(Dart 파서 하나를 빌드 스크립트와 앱이 같이
  쓴다 — 두 번째 구현 금지), 데이터 로딩 경로(저장본 우선, 없으면 번들), Android applicationId
  `io.github.brad0329.findchem`(바꾸면 설치본과 다른 앱), 화면 전환 구조(2026-09-11 Phase 004: 화면은 검색 하나(`lib/ui/search_page.dart`),
  헤더 `AppHeader`의 '⋮' 메뉴가 유일한 진입점 — 자주보는 Chem 목록(`lib/ui/favorites_page.dart`, 앱만)과 설정(`lib/ui/settings_page.dart`)은 이 메뉴에서 `Navigator.push`로 연다.
  현재 데이터는 `lib/data/dataset_loader.dart`의 `DataController`가 들고(저장본 우선, 없으면 번들 — 분기는 이 파일 한 곳)
  `lib/ui/app.dart`가 검색·설정 화면에 넘긴다). 동결 경계는 아직 없다.
- **저위험 실행**: 한 턴에 구현→검증(사다리대로)→**커밋**. **배포는 사용자 실테스트가 필요한 변경일 때만**
  하고 그때 실테스트 하나만 묻는다 — 주석·내부 로직 수정은 커밋에서 멈춘다. 보고 10줄 이내, 커밋은
  묻지 않는다. 작업 중 발견한 무관 결함이 5분짜리면 그 자리에서 고친다(커밋만 나눈다).

## CLAUDE.md 비대화 방지 ★이 파일에는 상한이 있다

- **이 파일은 매 세션의 컨텍스트에 통째로 실린다** — 규칙이 많을수록 규칙별 준수율이 떨어진다.
  단위는 바이트가 아니라 **개수**다: 최상위 모델도 지시문 150~200개 부근부터 준수율이 급락하고
  앞쪽 지시문을 더 잘 지킨다(IFScale 2025). Claude Code 시스템 프롬프트가 이미 약 50개를 쓴다.
  파일 크기 자체는 준수율에 효과가 없었다(Claude Code 세션 1,650건 요인 연구 2026) —
  종전 크기 상한(~15KB, 출처 88KB 실사례)은 2026-09-05에 개수 상한으로 교체.
- **상한: 규칙 100개** (초기화 블록 제거 후). 센다: `python scripts/count_claude_rules.py` —
  글머리·번호·표 행 하나 = 규칙 하나, `<...>` 플레이스홀더는 제외. 템플릿 출고 상태는 약 60개.
  Phase 종료 시 확인하고, 넘으면 이관한다:
  ① 구조(훅·테스트)로 강제된 항목은 한 줄+링크로 ② 정정의 역사·실험 전말은 플레이북으로
  (본문엔 현재 결론만) ③ 기능 상세는 REQUIREMENTS·Phase 로그로, 진행 서사는 plan.md로.
  남기는 것은 **"매 작업마다 적용되는 규칙 + 다음 세션이 첫 10분에 알아야 할 것"** 뿐이다.
- **중요한 규칙은 앞에 둔다.** 세션 후반의 망각은 개수로 못 막는다 — 그것은 훅이 막는다.
- **덧붙일 때 나갈 것을 함께 정한다** — 넣기만 하는 커밋이 반복되면 그것이 신호다. **규칙·결론을 바꿨으면
  같은 커밋에서 어긋난 문서를 grep으로 대조한다 — 범위에 `docs/`·README만이 아니라 `.claude/skills/`·
  `.claude/agents/`를 넣는다**(정정 3곳 중 스킬 문서만 빠져 이틀 뒤 발견된 실사례 — `노하우_프로세스.md`).

## 작업 로그 원칙 (`work_log/Phase_XXX.md`) ★이 템플릿에서 가장 값이 큰 부분
- **Phase 로그는 실패한 접근·버린 대안이 있을 때만, 그때 바로, 2절로 쓴다.** 없으면 파일을 만들지 않고
  plan.md 체크박스 옆에 완료일만(2026-09-05 사용자 결정). 뒤늦게 커밋에서 복원하면 시행착오는 소실된다.
  ① 실패한 접근과 원인("X 시도 → 실패 → 원인 Y → Z로 해결", 엉뚱한 데서 찾은 오해 포함)
  ② 다음 세션이 알아야 할 것(결정 이유+버린 대안·외부 제약·운영 주의점 — 있는 것만).
  코드 구현 상세·git 히스토리·테스트 목록·검증 결과는 쓰지 않는다 — 코드는 What, 로그는 Why.
- **로그에 적은 교훈은 저절로 다음 코드에 옮겨지지 않는다** — 가능하면 컴파일러·테스트·훅이
  잡는 구조로 강제하고, **문서는 구조로 강제할 수 없는 것에 쓴다.** 수동 등록부에는 대조 테스트를
  같은 커밋에. 실사례: `노하우_프로세스.md`

## 테스트 규칙
- 본 스택 테스트 명령: `flutter test`(전체만 — 파일 지정은 훅이 막는다. 출력을 줄이려면 `--reporter failures-only`),
  정적 분석 `flutter analyze`.
- 동봉 파이썬 도구(훅·측정기)의 테스트도 `tests/`에 있다(`python -m pytest tests/`, 규칙 개수 게이트 포함).
  본 스택이 파이썬이면 한 명령으로 같이 돈다. 아니면 **훅·측정기·CLAUDE.md를 고쳤을 때만** 따로 돌린다.
- **수용 기준 = 테스트 케이스**: REQUIREMENTS.md의 각 `[ ]` 항목이 테스트 하나에 대응해야 한다.
- 데이터 관련 기준은 **실제 데이터에 직접 질의해** 검증한다(모킹으로 대신하지 않는다).
- 외부 API에 의존하는 테스트는 **고정 응답으로 도는 것을 기본**으로 두고, 실호출은 분리한다.

## 배포 체크리스트
**"배포"가 무엇인지는 프로젝트마다 다르다**(서버 배포/앱 설치/패키지). 세팅 Phase에서 채운다.

1. 검증 — **트랙별로 다르다**: 저위험은 커밋 때 사다리대로 한 것 위에 **아무것도 더 얹지 않는다**
   (로직이 바뀐 커밋이면 전체 1회가 이미 돌았다). 일반 트랙은 사다리의 일반 트랙 갈래 전부
   (전체 테스트·구버전 데이터 마이그레이션 실측·되돌리기 경로·사용자 실테스트).
2. APK `flutter build apk --split-per-abi` → `build/app/outputs/flutter-apk/app-arm64-v8a-release.apk`
   (릴리스 서명 키는 `android/key.properties` — 없으면 빌드가 거부된다. `팩_Flutter_Android.md`)
   친구 배포는 pubspec 버전을 올리고 통합 APK를 GitHub Releases에 올린다(F-006, 절차는 같은 플레이북 '친구 배포')
3. APK: USB 연결 후 `C:/Users/user/AppData/Local/Android/Sdk/platform-tools/adb.exe install -r <위 APK>` — 설치 전
   APK의 LastWriteTime이 방금 빌드인지 본다(`팩_Flutter_Android.md`).
   **웹은 손으로 빌드·업로드하지 않는다** — master에 push하면 GitHub Actions가 빌드해
   https://brad0329.github.io/findchem/ 에 올린다(`.github/workflows/deploy-web.yml`)
4. **실제 동작 확인** — 실기기에서 앱 실행(런처 이름·아이콘, 검색 결과) / 웹은 배포 주소를 브라우저로 열어 같은 확인
5. commit (+ 원격이 있으면 push)

## 알려진 함정 (여기엔 스택 중립만. 스택 의존 함정은 `docs/playbooks/팩_*.md`에 있고, 새로 겪으면
`_TEMPLATE.md` 형식으로 파일 추가 + 여기 한 줄 — 함정 목록은 프로젝트가 진행되며 만들어진다)

- **명령 앞에 무엇도 붙이지 말 것 — 한 호출 = 한 명령**(cd 접두사는 훅이 막는다). **허용된 앞머리로
  뒤엣것을 통과시키는 우회 금지 — 속도보다 우선.** 환경변수는 `settings.json`의 `env`에.
  상세: `노하우_승인_대기_최소화.md` §4
- **문서에 적힌 API·필드를 기억으로 쓰지 말 것**: 실제로 열어 이름·건수를 확인한다.
  200이 성공이 아닐 수 있다. 상세: `노하우_외부API_데이터.md`
- **같은 원인의 버그를 고칠 때 다른 층에 같은 판정이 있는지 함께 본다**: 한쪽만 고쳐 재발한 실사례.
- **웹 지원 플러그인을 추가하면 첫 `flutter build web` 전에 `flutter clean`** — 증분 빌드가 옛
  `web_plugin_registrant.dart`를 재사용해 그 플러그인만 빠진 채 빌드되고, 누르는 즉시 MissingPluginException이
  난다(Android는 멀쩡해서 더 늦게 발견된다). 확인은 등록 파일의 `registerWith` 목록(`팩_웹_PWA.md`, 2026-09-12).
- **웹 빌드 asset은 ASCII 파일명, 재확인 전 서비스워커 해제** — 한글 파일명은 경로 길이로 빌드 실패, 옛 빌드가 캐시로
  뜬다(`팩_웹_PWA.md`, 2026-09-11).
- **PDF 파서를 갈아야 할 때 후보를 다시 조사하지 말 것** — 검토하고 버린 후보와 사유(Dart 6종·서버측 4종·상용 API)는
  `docs/playbooks/팩_PDF_표_파싱.md`. 우리 파서의 실측·함정은 `work_log/Phase_003.md`.
- `<프로젝트 진행하며 발견한 함정을 여기 한 줄 + 플레이북 링크로 추가>`
