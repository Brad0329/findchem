# FindChem 전체 계획 (단일 원본)

> PLAN MODE에서 최초 작성/주요 변경 시 갱신. Phase 완료 시 메인 agent가 체크박스 `[x]` + 완료일.
> `Phase_XXX.md`는 실패한 접근·버린 대안이 있을 때만 만든다.

## 시스템 개요
- FindChem은 물질명(국문·영문, 부분 일치, 띄어쓰기 무시)이나 CAS 번호를 넣으면 환경부 고시 「유해화학물질의
  규정수량에 관한 규정」에서 규정수량을 찾아 준다. 대상은 [별표 2] "인체·생태 유해성" 1,557건과 [별표 3]
  "사고대비물질" 100건이다. 사용자는 본인과 사내 소수다. 고시가 개정되면 각 기기에서 두 PDF를 올려 원천자료를
  바꾼다. 요구사항 상세는 docs/REQUIREMENTS.md에 있다.

## 아키텍처
- **Flutter 한 벌**로 Android APK와 웹을 만든다. 서버와 DB는 없다.
- **데이터**: 두 별표를 항목 단위로 담은 JSON 1개(약 561KB)를 앱에 번들한다. 앱이 시작할 때 메모리에 올려 검색한다
  (docs/SCHEMA.md).
- **파서**: **Dart PDF 파서 하나**를 두 곳에서 쓴다. ① 개발 PC에서 번들 JSON을 만들 때 ② 앱 안의 원천자료
  update(F-002). Python 파서는 두지 않는다. 같은 규칙이 두 곳에 생기기 때문이다.
- **원천자료 update 저장본**: 기기 저장소에 따로 둔다(Android는 앱 저장소, 웹은 브라우저 저장소). 저장본이 있으면
  그것을 쓰고, 없으면 번들본을 쓴다. 되돌리기는 저장본을 지우는 것이다.
- **외부 연동**: 없음. 화학물질 API는 프로젝트가 끝난 뒤 검토한다(보류 항목).

## Phase 체크리스트

> **가장 위험한 것을 먼저 한다.** "위험한 것" = 틀리면 되돌리기 비싼 것, 가능한지 아직 모르는 것.
> 쉬운 것부터 하면 나중에 못 하는 걸 발견한다.

- [x] Phase 001: 요구사항/설계 확정 — 완료 2026-09-11 (REQUIREMENTS F-001·F-002, SCHEMA, 두 PDF 전수 실측,
  아이콘 확정, 화학물질 API 실호출 3건. 시행착오는 `Phase_001.md`)
- [x] Phase 002: 프로젝트 세팅 — 완료 2026-09-11 (Flutter 3.35.6 저장소 루트·Android+웹, applicationId
  `io.github.brad0329.findchem`, 아이콘은 `scripts/make_icons.py`, 뼈대·작업 트랙 ②·테스트·배포 절을 CLAUDE.md에,
  `settings.local.json` 허용 규칙, 템플릿 초기화 블록·README·안 쓰는 팩 2개 정리. 시행착오는 `Phase_002.md`)
- [x] Phase 003: **Dart PDF 파서** — 완료 2026-09-11 (pdf_graphics 4.4.0(순수 Dart) 추출 층 + 정규화 층이 pdfplumber
  정답지와 2,517행 전수 일치, 번들 JSON 1,657건, 테스트 51건. 웹(Chrome) 2.6초·Android 실기기(SM, AOT) 284ms 모두
  "일치 1657/1657". 시행착오·버린 대안은 `Phase_003.md`)
  - ~~먼저 조사한다(`researcher`)~~ 완료 — 조사 결론은 `Phase_003.md`
  - 실측 기준 `Phase_001.md` 불변식 — 전부 테스트로 걸었다(수치 정정 3건은 `Phase_003.md`). 웹·Android 통과
  - 데이터 검증 테스트(`test/data/bundled_data_test.dart`)에 원문 오류 예외(괄호 4건·체크디지트 2건)를 넣었다
  - ~~Phase 종료 QA 게이트 뒤: 그 서브에이전트 트랜스크립트에서 `git -C` 호출이 사라졌는지 본다~~ 확인 2026-09-11:
    qa-tester 트랜스크립트의 실행 명령에 `git -C` 0건(git은 status·ls-files·check-ignore·ls-tree만). [H16] 처방 유효
- [x] Phase 004: F-001 물질 검색 — 완료 2026-09-11 (화면 방향 2~4·검색 시점·헤더 사용자 결정 → REQUIREMENTS. 검색 엔진
  `lib/search/`, 화면 `lib/ui/`, 로딩 경로 `lib/data/`. 수용 기준 11+1 전부 테스트(실데이터 질의), 변이 확인 3건, 웹(Chrome)
  라이트·다크·1열·2열 실확인. 시행착오는 `Phase_004.md`)
  - ~~게이트: 첫 화면 전에 REQUIREMENTS '화면 방향' 2~4를 묻는다~~ 완료 — 답은 REQUIREMENTS '화면 방향'
  - ~~수용 기준을 모두 테스트로 건다. 확정 목업이 기준이다~~ 목업 파일은 저장소에 없다 — 수용 기준 문장이 정답(REQUIREMENTS)
- [x] (Phase 005 전 끼워 넣은 소기능) F-003 카드 공유 — 완료 2026-09-11(실기기 공유 창 사용자 확인 통과).
  `lib/share/`(텍스트 형식 `share_text.dart`, 공유 창→클립보드→실패 알림 `share_action.dart`), 의존성 share_plus 12.0.2 추가(사용자 승인)
- [x] Phase 005: F-002 원천자료 update — 완료 2026-09-12 (설정 화면에서 PDF 2개를 올리면 같은 Dart 파서로 파싱해
  기기 저장본으로 교체, '처음 데이터로 되돌리기'. 자동 테스트 104건·변이 확인 8건, 폰·웹 사용자 확인 통과.
  Phase 통합 테스트(qa-tester) 합격: analyze 0건·test 104건·pytest 150건·클린 웹 빌드. 시행착오는 `Phase_005.md`)
  - ~~게이트: PDF 업로드는 첫 외부 입력 진입점이다. 들어가기 전에 REQUIREMENTS '비기능 요구사항' 1(인증 방식)을 묻는다~~
    완료 2026-09-12 — 인증 없음, 웹 파싱 중 멈춤 허용(비기능 1·3). 의존성 file_picker 추가·path_provider·web 직접 의존(사용자 승인)
  - 코드: 판정 `lib/data/source_update.dart`, 저장본 자리 `lib/data/update_store*.dart`, 로딩 분기·`DataController`
    `lib/data/dataset_loader.dart`, 화면 `lib/ui/settings_page.dart`
  - (2026-09-12 사용자 지시) 화면 확인은 사용자가 한다 — 웹은 빌드까지, 앱은 빌드·설치까지. AI는 스크린샷·브라우저 조작을 하지 않는다
  - 보안 3층 ②: 파일 업로드를 넣는 커밋은 그 범위만 점검한다(`/security-review`, 원격이 없으면 직접). 결과는 커밋 메시지에 적는다
  - 저장본을 넣는 것은 저장 파일을 **하나 더 늘리는** 변경이다. 번들본 형식은 바꾸지 않는다
- [ ] Phase 006: 배포 — APK 빌드와 실기기 설치, 웹 호스팅 위치 결정과 배포, 사용자 실테스트.
  공개 범위가 사내 소수라서 보안 3층 ③ 전체 리뷰를 할지는 이 Phase에서 호스팅 방식을 보고 사용자에게 묻는다
  - **첫 배포 전에 릴리스 서명 키를 정한다.** 지금 릴리스 APK는 Flutter 기본대로 debug 키(PC마다 다름)로 서명된다.
    키가 바뀌면 이미 설치된 앱 위에 업데이트 설치가 안 되고, 지우고 다시 깔아야 한다
- [ ] Phase 007: 부채 정리 — `/debt-audit` 2026-09-12 결과. **사용자 결정 2026-09-12: 정리 Phase로 간다.**
  훑은 범위: Phase 로그 5·플레이북 9·커밋 24 / 코드·테스트 약 58개 파일 / 문서 25개+코드·설정.
  항목별로 커밋을 나누고, 기계적 분리는 공개 표면을 유지해 테스트를 안 고치는 방식으로(`노하우_프로세스.md`)
  - **A. 구조 강제(1순위)**: A1 웹 산출물 검사 스크립트 — 등록 파일의 `registerWith` 목록 + `main.dart.js`의 플러그인
    문자열 확인(낡은 산출물·등록 누락 **4회**, 문서 3곳에 적고도 재발, 전부 사용자 실테스트에서 적발) /
    A2 기능별 "실패 분기 → 문구" 전수 표를 위젯 테스트로(2기능: F-003 조용한 실패, F-002 표시 결함 3건) /
    A3 실측은 커밋되는 `scripts/oracle_*` 형태로만(2회, 대가는 수치 정정 3건)
  - **B. 대조 테스트 없는 중복**: **B1 CAS 자릿수가 `parser/cas.dart`와 `search/search.dart`에 손으로 재계산돼 두 벌(일반)**
    / ~~B2 공백 제거가 검색은 전각 포함·파서는 미포함 — 이미 동작이 다름(일반)~~ **전제 오류, 정정 2026-09-12(실측)**:
    Dart 정규식은 ECMAScript라 `\s`가 이미 U+3000을 포함한다(일회성 probe 테스트로 확인 — `RegExp(r'\s')`가 U+3000에
    match, `\s+` 제거가 U+3000도 지우고 `String.trim()`도 지운다. 전체 105건 통과 후 probe 삭제). 파서 `_squash`(`\s+`, 머리글 대조 전용)와
    검색 `normalize`(`[\s　]+`, 질의·이름)는 **동작이 같고** `search.dart`의 `　`는 중복 표기일 뿐이다 → 저위험, 남은 일은
    전각 공백 케이스를 테스트로 걸어 두는 것 정도(합치는 이득은 작다 — 쓰임이 다르다) / B3 PDF 날짜 포맷 2구현(파싱 실패 시
    한쪽은 생략·한쪽은 원문 노출) / B4 카드↔공유의 번호 라벨·CAS 줄 복사(테스트까지 3번째) / B5 아이콘 SVG↔`BrandSymbol`
    수기 복제, 대조 없음(일반) / B6 assets PDF 찾기 3벌 / B7 테스트의 건수 리터럴 6파일·번들 로드 8곳·문구 형식 복제 /
    B8 파이썬 훅 테스트 3벌(일반)
    - ~~B1·B2는 동작이 갈리는 중복이라 배포(Phase 006) 앞으로 당길 수 있다~~ **정정 2026-09-12: 당길 근거가 없다.**
      B2는 전제 오류(위), **B1도 현재 두 값이 일치해 보이는 결함이 없다** — 영향은 0건 안내 문구 하나뿐이고
      (검색 매칭은 `[0-9-]+`+앞부분 일치라 자릿수와 무관) 고시 개정으로 CAS 앞부분 자릿수가 바뀔 때 한쪽만 고치면
      REQUIREMENTS F-001의 '묶음 항목 안내' 기준이 조용히 깨진다. 그 드리프트를 잡는 테스트가 없는 것이 B1의 값이다
  - **C. 복수 원본(이미 어긋난 것)**: 복수 CAS 55(SCHEMA)↔56(테스트) / 괄호 짝 오류 3(Phase_001)↔4(코드·Phase_003) /
    JSON 크기 문서 561KB↔**실측 619,256바이트(605KiB)**·gzip 88KB↔약 91KiB / CLAUDE.md '현재 단계'가 상태를 중복해
    매번 어긋남(오늘도) → 상태 원본은 plan.md(Phase)·REQUIREMENTS(기능) 둘만 두고 그 줄은 링크로 /
    병기 링크화 8건(데이터 로딩 경로 4곳·건수 6곳+테스트 6파일·applicationId 4곳·CLAUDE.md의 아키텍처 상술 등)
  - **정상 확인(손대지 않는다)**: 파서↔pdfplumber 정답지 대조 테스트 있음, `lib/parser` 공유 지켜짐(두 번째 구현 0건),
    테스트·빌드·배포 명령과 앱 이름·시드색은 문서·코드 전부 일치, CLAUDE.md 규칙 71/100개
  - ~~PWA 남색(#0B1F4F)이 "심볼 안에서만"과 어긋남~~ **결정 2026-09-12: 문서에 예외를 적는다**(REQUIREMENTS '화면 방향' 3에
    반영 완료) — `web/manifest.json`은 그대로 둔다
- [ ] 운영 단계 전환 (CLAUDE.md 정책 "운영 모드"로 갱신)

## 결정된 것 / 미정인 것

**확정**
- (2026-09-11, 사용자 결정) **데이터 원천 = `assets/`의 고시 별표 PDF 2개를 파싱해서 쓴다.**
  외부 API로 대체하는 안은 조사 도중 중단했다 — 사유: 조사 시간. 이후 확인해 보니 API에 규정수량이 없어서
  결정이 유지된다(보류 항목).
- (2026-09-11) **서버 DB는 두지 않는다.** 근거: 읽기 전용이고 사용자 데이터가 없다. 1,657건, JSON 약 561KB
  (gzip 88KB, 전수 실측). 정규화 데이터를 앱·웹에 정적으로 넣는다. 파싱 시 함정은 `Phase_001.md`에 있다.

- (2026-09-11, 사용자 결정) **스택 = Flutter 한 벌로 Android와 웹을 함께 만든다.** 버린 대안은 둘이다.
  웹 PWA 하나는 스토어 앱이 아니고, 네이티브와 웹을 따로 만들면 검색 규칙이 두 곳에 구현된다.
- (2026-09-11, 사용자 결정) **사용자 = 본인과 사내 소수.** APK를 직접 설치하고, 스토어에는 등록하지 않는다.
- ~~(2026-09-11) 고시가 개정되면 새 PDF를 다시 파싱하고 앱·웹을 재배포한다~~ → **같은 날 사용자 결정으로 대체:
  앱 설정의 '원천자료 update'(F-002)에서 PDF를 올리면 앱이 파싱해서 교체한다.** 원격 JSON 갱신은 여전히 하지 않는다.
  - 결과 1: **PDF 파서는 Dart로 하나만 둔다.** 앱에 넣는 JSON도 같은 Dart 파서로 만든다. Python 파서를 따로 두면
    같은 규칙이 두 곳에 생긴다.
  - 결과 2: ~~가장 위험한 항목은 "Dart(Android·웹)에서 이 PDF의 표를 pdfplumber만큼 추출할 수 있는가"이고, 아직
    모른다~~ → **해소(2026-09-11, Phase 003)**: pdf_graphics 4.4.0(순수 Dart)로 pdfplumber와 셀 단위 전수 일치.
    VM·웹(Chrome)·Android 실기기에서 확인. 라이브러리 선택은 사용자 결정, 근거는 `Phase_003.md`.
  - 결과 3: 올린 PDF로 만든 원천자료는 기기에 저장한다. 앱에 번들한 데이터와는 따로 둔다(SCHEMA.md).
- (2026-09-11, 사용자 결정) 앱 이름은 **FindChem**이다. 화면 용어는 별표2 → "인체·생태 유해성", 별표3 → "사고대비물질"이다.
- (2026-09-11, 사용자 결정) 검색 결과 화면은 목업대로 확정했다. 이름은 부분 일치로 찾는다. 두 표에 모두 있으면
  사고대비물질을 위에 두고 '우선 적용'을 표시하며, 인체·생태 유해성 값은 참고로 함께 보여준다.

- (2026-09-11, 사용자 결정) **CAS 검색 결과가 0건이면 안내를 띄운다: "염류·화합물 묶음 항목에 해당할 수 있으니
  이름으로도 검색해 보세요".** 이유: 원천 PDF에 CAS 없는 항목이 77건 있고, 어떤 CAS가 어느 묶음에 속하는지는
  PDF에 없다. F-001 수용 기준에 반영했다.

**미정**
- 웹을 어디에 호스팅할지(정적 호스팅) — Phase 006
- **웹 화면을 표 형식으로 갈지(앱은 카드 유지)** — 사용자 요청 2026-09-12, 목적은 **Excel로 긁어 붙이기**.
  실측 먼저 함(`test/ui/selection_copy_probe_test.dart`, 값은 그 테스트에 못박음):
  ① `SelectionArea`는 여러 Text에 걸친 선택을 **구분자 없이** 이어 붙인다 → 셀마다 Text인 표는 Excel에 붙이면
  한 칸에 다 들어간다(열·행 구분 소실). ② 한 Text 안의 `\t`·`\n`은 그대로 복사된다 → **한 덩어리 Text = TSV**여야
  열이 갈린다. ③ 따라서 선택지는 (가) 표를 행 단위 이상 한 Text로 그리기(정렬은 고정폭 글꼴/폭 맞춤 필요)
  (나) '표 복사' 버튼으로 TSV를 클립보드에 직접 넣기(가장 확실, 드래그 불필요) (다) 둘 다.
  남은 실측: 브라우저에서 드래그·Ctrl+C·Excel 붙여넣기가 실제로 되는지(사용자 확인). 화면이 두 벌이 되므로
  **일반 트랙** — 착수 전 사용자 결정 필요(REQUIREMENTS에 웹 화면 절·수용 기준을 따로 써야 한다)
- ~~화면 방향 2~4(기준 기기, 룩앤필, 다크모드·접근성) — Phase 004 게이트~~ 확정 2026-09-11(REQUIREMENTS '화면 방향')
- ~~인증 방식(비기능 1) — Phase 005 게이트~~ 확정 2026-09-12: 없음(REQUIREMENTS 비기능 1)

## 사용자 실테스트 대기
- ~~(F-002, Phase 005 완료 조건) Android 실기기·웹에서 적용·유지·고장 상태·되돌리기~~ **통과 2026-09-12** — 폰(R5KL70B792J)에
  arm64 APK 설치 후 사용자 확인(건수 1,557/100 일치, 고장 상태 2건), 웹은 로컬 서버(build/web)에서 같은 흐름 확인.
  웹은 첫 빌드에서 file_picker 웹 등록이 빠져 실패 → `flutter clean` 후 재빌드로 통과(`Phase_005.md`)
- ~~(F-003) Android 실기기에서 카드의 공유 아이콘 → 시스템 공유 창이 뜨고, 카톡 등에 붙은 텍스트가 REQUIREMENTS F-003 예시 모양인가~~
  **통과 2026-09-11** — arm64 APK(46f38d8)를 R5KL70B792J에 설치, 사용자가 직접 확인. 웹은 AI가 확인: 앱 내장 브라우저(공유 API·클립보드 둘 다 막힘)에서 "공유하지 못했습니다" 알림, 375px 1열·800px 2열 아이콘 위치
- ~~(Phase 004) 실기기에서 검색 화면~~ **통과 2026-09-11 21:55** — arm64 APK를 R5KL70B792J에 설치, 사용자가 직접 확인
  ("app에선 잘나오는듯"). 하이픈 없는 CAS('96297', d88399d)도 22:05 실기기 확인 통과. 웹은 로컬 서버(build/web)만 —
  호스팅·배포는 Phase 006. Chrome 1024px·375px 에뮬레이션은 AI가 확인
- (F-002, Phase 006 설치 때 함께 — qa-tester 2026-09-12 지적) 앱을 **새 버전으로 덮어 설치한 뒤에도 저장본이 남고
  계속 우선하는가**(REQUIREMENTS AI 기본값). 자동 테스트로는 못 건다
- (F-002, 낮은 위험) 웹 localStorage **한도를 넘겼을 때** '저장하지 못했습니다'로 끝나고 기존 저장본이 남는가 —
  코드는 그렇게 돼 있으나 실브라우저 미확인(현재 약 0.6MB / 한도 5MB)
- (Phase 002, Phase 006 설치 때 함께) 실기기 런처에 이름 FindChem과 아이콘이 뜨는가 — 적응형 아이콘이 원형 마스크에서
  잘리지 않는가. 웹은 탭 제목·파비콘·PWA 설치 아이콘(maskable)이 맞게 보이는가
- ~~(Phase 003 완료 조건) Android 실기기에서 파서 결과가 번들과 같은가~~ **통과 2026-09-11 21:08** — 실기기(R5KL70B792J)에
  확인용 APK 설치, 화면 "일치 1657/1657", 별표2 56쪽 284ms·별표3 17ms. 재확인 절차는 `lib/dev/parse_check_main.dart` 머리 주석.

## 보류 항목 (나중에 할 것)

> 조사만 하고 미룬 것을 여기 남긴다. **다시 조사하지 않아도 되도록 실측 결과와 근거까지** 적는다.
> "안 되더라"가 아니라 "왜 안 되는지, 무엇까지 확인했는지"를 적어야 값이 있다.

- **외부 원천 조사(중단, 2026-09-11)** — 대상: law.go.kr 고시 연혁·DRF API, NCIS, 공공데이터포털
  API·파일데이터, KOSHA MSDS API. 결과를 받기 전에 중단했다. 그 뒤 API만 따로 확인했다(아래). 알려진 것:
  ① PDF 본문에 고시 번호·시행일이 없다. 메타데이터 생성일은 2026-07-20이고, 고유번호 최대값은 `2026-1-1353`이다.
  ② law.go.kr은 WebFetch로 본문을 가져오지 못했다. 재개 시점(2026-09-11 사용자 결정): **이 프로젝트가 끝난 뒤
  API 추가를 다시 검토한다.** 첫 단계는 law.go.kr 행정규칙 연혁을 브라우저로 열어 이 PDF가 최신판인지 확인하는 것이다.
  - **API 문서 확인(2026-09-11)**: 공공데이터포털의 Swagger 명세와 목록 페이지를 봤다. 15149420만 실제로 호출했다(아래 '실호출 결과').
    - [한국환경공단_화학물질 정보 조회 서비스](https://www.data.go.kr/data/15149420/openapi.do)
      - 관리부서는 화학물질관리처 화학물질정보부다. 이 부서가 화학물질정보처리시스템을 운영하는 곳으로 보인다(추정).
      - CAS·물질명·고유번호로 검색한다. 응답은 `casNo`, `korexst`, `sbstnNmKor/Eng`, 분자식·분자량,
        `typeList[]{sbstnClsfTypeNm 물질분류유형명, unqNo 고유번호, contInfo 함량정보, excpInfo 예외정보, ancmntYmd 고시일자,
        ancmntInfo 고시정보}`이다.
      - **규정수량 필드는 없다.** 인증키가 필요하고, 개발 계정 한도는 10,000건이다. 최종 수정일은 2025-09-16이다.
    - [한국환경공단_유독물GHS 정보 조회 서비스](https://www.data.go.kr/data/15149423/openapi.do): 신호어, 그림문자, UN번호,
      M계수, 유해성분류를 준다. 규정수량은 없다.
    - [화학물질안전원_화학물질안전관리정보](https://www.data.go.kr/data/15072442/openapi.do): 응급 증상(흡입, 피부 등)을 준다.
      규정수량은 없다.
    - **실호출 결과(2026-09-11, 표본 3건)**: 15149420 `chemSbstnList`를 불렀다. 키는 `.env`의 `DATA_GO_KR_KEY`에 있다.
      - `searchGubun=3`(고유번호), `searchNm=97-1-4` → **0건**. 고유번호 검색이 이 형식을 받지 않는 것으로 보인다. 원인은 모른다.
      - `searchGubun=2`(CAS), `13516-27-3` → 구아자틴이 나온다. `typeList`에 `{sbstnClsfTypeNm:"인체등유해성물질",
        unqNo:"97-1-4", contInfo:"인체급성유해성 : 1%, 생태유해성 : 25%", ancmntInfo:"화학물질안전원고시 제2025-19호"(20250807)}`가 있다.
        **PDF의 고유번호·함량기준(급성 1, 생태 25)과 일치한다.**
      - `searchGubun=2`(CAS), `7758-95-4`(염화납 — PDF에 개별 항목이 없다) → `typeList`에 `{sbstnClsfTypeNm:"인체등유해성물질 세분류",
        unqNo:"97-1-9", contInfo:"인체만성유해성 : 0.3%, 생태유해성 : 25%"}`가 있다. **묶음 항목("납과 그 화합물")에 속하는지를
        API가 알려 준다 — 가설 확인(표본 1건).** 같은 응답에 제한물질(`06-5-8`), 중점관리물질, 암·돌연변이성물질 분류도 온다.
      - 남은 문제: PDF에서 `97-1-9`는 21행이 함께 쓴다(납과 그 화합물, 무기 납화합물 등). 어느 행의 규정수량을 적용할지는
        고유번호만으로는 정해지지 않는다. `contInfo`의 구분·함량으로 행을 고르는 규칙이 필요하다. 규정수량 값 자체는
        API에 없어서 PDF가 여전히 원천이다.
