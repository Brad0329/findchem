# MCP 서버 spike ③ — 질문 세트와 기록

> 계획·확인 항목의 단일 원본은 `plan.md`의 '보류 항목 · MCP 서버 spike'다. 여기엔 ③단계(데스크톱 Claude에
> 걸고 실제 질문을 던지는 단계)에 필요한 것만 둔다. 이 파일은 spike/mcp-server 브랜치에만 있다.

## 등록

**이 PC에는 2026-09-16에 등록해 두었다.** `%APPDATA%\Claude\claude_desktop_config.json`의 `mcpServers`에
아래 항목을 더했다(기존 서버 2개는 그대로. 직전 파일은 같은 폴더에 `.bak-20260916`으로 남겼다).

```json
"findchem": {
  "command": "C:\\Program Files\\Python311\\python.exe",
  "args": ["C:\\Users\\user\\Documents\\findchem\\scripts\\mcp_server.py"],
  "env": { "PYTHONUTF8": "1" }
}
```

- `python`이 아니라 **인터프리터 절대경로**를 쓴다 — GUI 앱은 셸 PATH를 그대로 받지 않는다(기존 두 서버도 절대경로다).
- `dart`는 머신 PATH의 `C:\flutter\bin`에 있어 데스크톱 앱이 물려받는다(확인 2026-09-16). 못 찾으면
  `search_chemical`이 "dart 실행 파일을 PATH에서 찾지 못했습니다"로 **소리내어** 실패한다.
- 등록 전후로 `python scripts/mcp_server.py --selftest`가 통과하는지 본다(설정에 적은 그 인터프리터로도 확인했다).
- 데스크톱 Claude는 설정을 바꾼 뒤 **완전히 종료했다가 다시 켜야** 서버를 다시 읽는다.

## 모델 ★먼저 정하고 기록한다 (2026-09-16)

**질문에 답하는 모델은 데스크톱 Claude에서 선택된 모델이다** — 서버는 도구만 주고 모델을 정하지 않는다.
그런데 확인 ②(도구를 부르나)·③(값을 뭉개나)는 **모델 능력에 직접 걸리는 항목**이라, 한 모델에서 통과한 결과가
다른 등급에 그대로 전이되지 않는다. 그러므로:

- **앱에 쓸 모델로 돌린다.** 권고 순서 = Opus 5로 한 번(상한 확인) → 앱 후보 모델로 같은 질문 12개 한 번.
  차이가 나면 그 자체가 결론이다("이 기능에는 이 등급이 필요하다")
- 앱에서는 모델 ID를 우리가 지정한다. 2026-06-24 기준 현행 ID·가격(100만 토큰당 입력/출력):
  `claude-opus-5` $5/$25 · `claude-sonnet-5` $2/$10 · `claude-haiku-4-5` $1/$5
- **같은 모델이어도 데스크톱 Claude에는 자체 시스템 프롬프트가 있어 앱과 동일하지 않다** — 근사 전이다.
  앱에서는 시스템 프롬프트·도구 설명을 우리가 쓴다(`mcp_server.py`의 `instructions`와 도구 docstring이 그 초안)
- Flutter/Dart에는 공식 Anthropic SDK가 없어 앱은 raw HTTP로 부른다(`http` 패키지는 F-007에서 이미 의존성)

**돌릴 때마다 아래 기록 표 머리에 모델 이름을 적는다.** 안 적으면 나중에 결과를 해석할 수 없다.

## 보는 법 ★답만 보면 안 된다

질문마다 세 칸을 채운다. **답이 맞아도 도구를 안 불렀으면 실패다** — 이번에 맞은 것은 다음에 틀린다.

| 칸 | 무엇 | 실패 모양 |
|---|---|---|
| 호출 | 도구를 불렀나 (호출 로그) | 부르지 않고 아는 것처럼 답한다 ← 가장 위험 |
| 값 | 검산과 맞나 | 합산을 빠뜨린다 / 잘못된 단을 고른다 |
| 인용 | 받은 값을 그대로 옮겼나 | 반올림·단위 누락·별표(*) 표시 누락 |

## 검산용 실측값 (2026-09-16 `dart run scripts/search_cli.dart`로 뽑음 — 톤)

| 물질 | CAS | 별표3(사고대비물질) 최하위 | 별표2(인체·생태) |
|---|---|---|---|
| 톨루엔 | 108-88-3 | 0.05 (함량 85%) | 연번 439 **(삭제)** |
| 메틸알코올 | 67-56-1 | 0.05 (85%) | 연번 107 급성 10% → 0.125 |
| 황산 | 7664-93-9 | 0.125 (10%) | 연번 581 급성 10% → 0.125 |
| 암모니아 | 7664-41-7 | 0.05 (10%) / **암모니아 용액 0.5\*** | 연번 282 급성·생태 0.05, 용액 0.5 |
| 염화수소 | 7647-01-0 | 0.02 (10%) / **염화수소 용액 0.2\*** | 306 염화 수소 0.02 / 307 염산 0.2 |
| 아세톤 | 67-64-1 | **없음(0건)** | 없음 |

## ⓐ 합산이 성립하는 질문 (별표 4 제3호 다목 — 보관시설만 설치·운영하는 사업장)

- **A1** "유해화학물질 보관시설만 운영하는 사업장입니다. 창고에 톨루엔 0.01톤, 황산 0.05톤,
  염화수소 0.004톤을 보관합니다. 최하위 규정수량 미만인가요?"
  검산: 0.01/0.05 + 0.05/0.125 + 0.004/0.02 = 0.2 + 0.4 + 0.2 = **R 0.8 → 1 미만이므로 미만이 맞다**
  실패: 물질별로 각각 미만이라고만 하고 합산을 안 한다 / 보유량 총합(0.064톤)을 더한다
- **A2 (경계값)** "같은 창고에 톨루엔 0.02톤, 황산 0.05톤, 암모니아(기체) 0.01톤이면?"
  검산: 0.4 + 0.4 + 0.2 = **R 1.0 → '1미만'이 아니므로 미만이 아니다**
  실패: R=1.0을 "미만"으로 본다(원문은 "R값이 1미만에 해당하는 경우에만")
- **A3 (적용 범위 밖)** "우리는 제조·사용시설도 같이 운영합니다. 같은 물질·같은 양이면 합산으로 판단하나요?"
  정답: 다목은 **보관시설만을** 설치·운영하는 사업장 한정이라 이 사업장에는 쓰지 않는다
  실패: 조건을 확인하지 않고 그대로 합산한다

## ⓑ 합산이 성립하지 않는 질문 — **정답은 숫자가 아니라 되묻는 것**

- **B1** "톨루엔 3톤 있는데 규정수량 넘나요?"
  정답: 어느 단(최하위 0.05 / 하위 2 / 상위 400)을 묻는지, 함량 85% 이상인지, 시설유형이 무엇인지를 되묻는다
  실패: 한 값을 골라 "넘습니다/안 넘습니다"로 단정한다
- **B2** "암모니아 0.3톤이면 규정수량 넘나요?"
  정답: 기체(0.05)냐 **암모니아 용액(0.5\*)**이냐로 답이 갈린다 — 성상을 되묻는다
- **B3** "염산 0.5톤은요?"
  정답: 염화 수소(0.02)·염산(0.2)·별표3 염화수소 용액(0.2\*) 중 무엇인지 갈라야 한다

## ⓒ 확인 ② — 도구를 부르나

- **C1** "아세톤 5톤 보관 중인데 규정수량이 얼마죠?" → 도구는 **0건**을 돌려준다.
  정답: 이 고시 별표 2·3에 개별 항목이 없다고 답하고 묶음 항목 안내(hint)를 전한다. 값을 지어내면 실패
- **C2** "황산 규정수량 알려줘" → 값이 유명해 기억으로 답하기 쉽다. **호출 로그로만 판정한다**
- **C3** "이 답은 고시 몇 호 기준이야?" → 도구 응답의 `source.notice`(제2025-12호, 2025-08-07 시행)를 인용해야 한다

## ⓓ 확인 ③ — 받은 값을 뭉개나

- **D1** "염화수소 최하위 규정수량은?" → **0.02톤** 그대로. 0.2와 섞거나 "약 0.02톤쯤"은 실패
- **D2** "암모니아 용액 규정수량은?" → 원문이 **`0.5*`**다. 별표(\*) 표시를 떼고 말하면 원문 표시가 사라진 것 — 실패로 본다
- **D3** "메틸알코올은 어느 표 기준으로 보나요?" → 두 표에 다 있고 **사고대비물질(별표3) 우선 적용**,
  함량기준이 85%(별표3) / 10%(별표2)로 다르다는 것까지 나와야 한다

## 결과 기록

> 실행한 뒤 여기에 적는다. 확인 ①②③의 답이 나오면 spike는 끝이다 —
> 쓸모가 확인되면 F-008로 정식화하고, 아니면 폐기한다(plan.md).

**모델: Claude Sonnet 5 (API ID `claude-sonnet-5`)  /  실행일: 2026-09-16  /  띄운 곳: PC 데스크톱 Claude 새 대화**

> 앱에 쓸 후보 모델을 먼저 돌린다(사용자 결정 2026-09-16 — 계획의 "Opus 5 먼저"와 순서를 바꿨다).
> **여기서 통과하면 Opus 5는 돌릴 필요가 없다.** 떨어지면 그때 Opus 5로 한 번 돌려,
> 실패 원인이 **모델 등급**인지 **설계(도구 설명·응답 형식)**인지를 가른다.

| 질문 | 호출 | 값 | 인용 | 메모 |
|---|---|---|---|---|
| A1 | O | O | △ | get_rule 먼저 → 물질 3건 각각 조회 → R 0.8 계산, 검산 일치. 수량·단위는 그대로 옮겼으나 **출처(별표 구분·연번·고시명) 미표기**(instructions에 적었는데 안 따름). 도구 검색 1회 헛침("규정수량 화학물질 조회" 0건) 후 재검색으로 찾음 |
| A2 | O | O | △ | 새 물질(암모니아)만 호출하고 앞서 받은 값 재사용. **R 1.0을 "1 미만 아님"으로 정확히 판정**(경계값 통과). 출처 미표기는 A1과 동일. **과잉 단정 1건**: "규정수량 이상 취급시설로 간주된다"는 원문에 없는 결론 — 다목은 특례 부적용일 뿐이고 물질별로는 전부 최하위 미만이다 |
| A3 | O(재사용) | O | O | 새 호출 없이 A1의 get_rule 원문을 사용 — 인용 문구가 다목 **원문과 글자까지 일치**(기억이 아님). **앞의 R 계산을 끌고 오지 않고** 예외 미적용으로 정확히 자름. 대안으로 든 "제조·사용+보관 보유량의 합" = 별표 4 제1호 그대로(제1호의 시설별 합과 제3호 다목의 물질 간 R 합산을 구분) |
| B1 | O(재사용) | **보류** | △ | ⚠ **A와 같은 대화에서 던져 오염됐다**(새 대화 지시가 지켜지지 않음) — 보관창고·톨루엔 값이 이미 깔려 있어 되물을 이유가 줄었다. **새 대화에서 재실행 필요.**<br>참고(오염된 상태의 관찰): 되묻지 않고 "하위 이상 상위 미만 구간"으로 답함. 하위 2·상위 400은 정확하나 **최하위 0.05톤 생략**, **함량기준 85% 미언급** |
| B2 | O(재사용) | **보류** | △ | ⚠ **오염 — 판정 철회.** A2 질문에 사용자가 **"암모니아(기체)"**라고 직접 썼다. 기체를 고른 것은 그 맥락을 이어받은 합리적 동작이고, "성상을 안 묻는다"는 결론의 근거가 되지 못한다. **새 대화에서 재실행 필요**(0.3톤은 기체 0.05 / 용액 0.5*가 갈리는 자리라 시험 가치가 크다) |
| B3 | O(재사용) | O | △ | **유효**(앞 대화에 없던 구분을 스스로 했다): "염산은 염화수소가 아닌 염화수소 용액 기준"이라고 명시하고 0.2/8을 정확히 고름. 별표(*)·함량·출처는 누락 |
| C1 | O | O | O | **지어내기 없음 — 가장 위험한 실패 모양을 통과.** 호출 블록 펼침 확인(`query:"아세톤"`, `total:2`). 걸린 2건이 **아세톤 유도체(아세톤 시아노히드린 등)**임을 알아채고 "별개 물질"로 자름(부분 일치 잡음을 답으로 옮기지 않음). 출처도 "화학물질안전원 고시 별표2·별표3"으로 밝힘<br>기록: **이름으로 검색하면 2건, CAS(67-64-1)로는 0건** → `hint`(묶음 항목 안내)는 CAS 질의에서만 뜬다. 실제 사용자는 이름으로 묻는다 |
| C2 | O | O | O | **새 대화에서 재실행한 결과**(첫 시도는 앞 대화 맥락이 있어 판정 불가 → 폐기). "로드된 도구, findchem 통합 사용함" 블록 표시(B 계열엔 아예 없었다). 답에 **CAS·함량기준 10%**가 들어 있는데 이는 도구 응답 `content` 필드에서만 나오는 값 → 도구를 읽은 증거. **유명한 값이어도 기억으로 때우지 않는다.** 연번·고시 번호는 여전히 누락 |
| C3 | O(재사용) | O | O | `source.notice` 문자열을 그대로 인용("제2025-12호(2025-08-07 시행)"). 기억으로 낼 수 있는 정보가 아니다.<br>**출처 약점의 성격을 규정해 준다**: 고시 정보는 모델에 도달하고 **물으면 정확히 쓴다** — 빠지는 것은 *스스로 붙이지 않는* 기본 동작뿐이라, 프롬프트·UI(근거 줄을 화면이 직접 렌더링)로 해결 가능한 종류다 |
| D1 | O | O | O | 새 대화. **0.02톤을 그대로 인용**(0.2와 혼동 없음, 반올림 없음) + CAS·함량기준 10%.<br>⚠ **도구 탐색 2회 헛침 재현**(A1에 이어 2번째): 한국어 서술형 질의("염화수소 규정수량 화학물질", "규정수량 조회 화학") 0건 → **도구 이름 그대로("findchem search_chemical")** 검색해서야 찾음. MCP 경로의 실비용(새 대화마다 헛검색이 앞에 붙는다) — 앱에 함수로 직접 묶으면 탐색 단계가 없어 생기지 않는다 |
| D2 | O | O | O | **인용 항목 최고 결과.** 별표(*)를 살렸고 **표별로 다르게** 옮겼다 — 별표2 용액 `0.5/20/400`(별표 없음), 별표3 용액 `0.5*/20*/400*`(별표 있음). 실제 데이터가 정확히 그렇다(뭉뚱그리지 않았다).<br>"(비고 적용)"이라고 별표의 뜻까지 밝혔다 = 응답의 **`legend.note`를 읽은 것** → **legend 필드가 값을 했다. 앱으로 옮길 때도 이 설명 블록을 함께 넘길 것** |
| D3 | O | △ | △ | 두 표 등재와 **함량기준 차이(별표2 10% / 별표3 85%)는 정확히 말했다.** 그러나 판정 근거로 **"더 엄격한 쪽(적은 규정수량)을 적용"이라는 고시에 없는 규칙을 지어냈다.** 실제 규칙은 사고대비물질(별표3) **우선 적용**(별표2 일반기준 가). 이번엔 별표3이 더 작아(0.05<0.125) 결론이 우연히 일치했을 뿐, 별표2가 더 작은 물질에서는 틀린다<br>**원인 = 우리 쪽 설계 결함**: 응답에 `priority`/`referenceNote` 불리언이 있는데 **`legend`에 그 설명이 없다.** D2(legend가 별표 해석을 살림)와 정확히 대칭 — **설명한 필드는 쓰이고 설명 안 한 필드는 버려진다** |
