# FindChem 요구사항 (단일 원본)

> 기능 요구사항 + 수용 기준의 단일 원본. **요구사항이 바뀌면 대화로 합의만 하지 말고 이 파일을 즉시 갱신.**
> 완료 판정은 항상 이 문서를 기준으로 한다(에이전트를 쓰든 직접 대조하든).
> **새 F-번호는 CLAUDE.md의 '요구사항 ID 대장'에서 받아간다. 번호 재사용 금지.**

## 시스템 목표
- 물질명이나 CAS 번호를 넣으면 화학물질안전원고시 「유해화학물질의 규정수량에 관한 규정」(2026-09-15 확인 현행: 제2025-12호,
  2025-08-07 시행)에서 그 물질의 규정수량을 바로 찾아 준다. 대상은 두 표다.
  - [별표 2] 인체급성·인체만성·생태유해성 물질별 규정수량
  - [별표 3] 사고대비물질별 규정수량
- 사용자는 본인과 사내 소수다. Android 앱과 웹으로 제공하며, 둘 다 Flutter 한 벌로 만든다. 서버는 없다.

## 용어 (화면 표시) — 2026-09-11 사용자 결정
- [별표 2] → **"인체·생태 유해성"**, [별표 3] → **"사고대비물질"**.
- 화면에는 이 두 이름을 쓰고 "별표2", "별표3"은 쓰지 않는다. 데이터 내부 식별자는 SCHEMA.md를 따른다.

## 사용자/권한
- 역할을 구분하지 않는다. 앱을 가진 사람은 누구나 조회할 수 있고 원천자료 update(F-002)도 할 수 있다.
  (AI가 채운 기본값이다. 달라지면 여기부터 고친다.)

## 화면 방향

> **게이트: 화면을 그리는 첫 기능을 구현하기 전에 이 절이 채워져 있어야 한다.** 비어 있으면 그 자리에서
> 사용자에게 묻는다(CLAUDE.md '요구사항 정책').

1. **UI 형태**: Android 앱 + 웹(Flutter 한 벌)
2. **기준 기기·해상도**: 휴대폰 세로(폭 360~412dp)가 기준이다. 넓은 화면(폭 900px 이상 — 웹 데스크톱)에서는
   결과 카드를 **2열**로 놓는다(2026-09-11 사용자 결정).
   - **앱과 웹의 결과 화면이 다르다**(2026-09-12 사용자 결정): **앱(Android)은 카드**, **웹은 표**(F-004).
     웹은 폭이 좁아도 표를 유지하고 **가로 스크롤**한다(카드로 바꾸지 않는다 — 화면을 세 벌로 만들지 않기 위해서).
     분기는 `kIsWeb` 한 곳(`lib/ui/search_page.dart`)에서만 한다.
3. **룩앤필의 기준**: **Material 3 + 아이콘 색 시드**(2026-09-11 사용자 결정). Flutter 기본 Material 3 위젯을 쓰고
   색은 아이콘의 파랑(#2E7FE0)을 시드로 Material 3가 자동 생성한다(남색 #0B1F4F는 심볼 안에서만 쓴다 — 시드는 하나뿐이라
   AI가 파랑을 골랐다). **예외: PWA(`web/manifest.json`)의 `theme_color`·`background_color`는 남색 #0B1F4F를 쓴다**
   (2026-09-12 사용자 결정 — /debt-audit이 "심볼 안에서만" 규칙과의 어긋남으로 지적, manifest를 그대로 두기로 했다).
   커스텀 디자인은 없다. 삭제 항목은 흐리게.
   2026-09-11 목업(검색 결과 카드, 구분별 수량 표)은 데이터 배치의 기준이다.
   - **결과 카드의 표 이름은 색 뱃지로 쓴다**(2026-09-12 사용자 결정, 구현 `lib/ui/entry_card.dart`):
     사고대비물질은 테마 강조색(`colorScheme.primary` + `onPrimary`), 인체·생태 유해성은 붉은색(#C62828) + 흰 글자.
     붉은색은 시드(파랑)에서 나오지 않아 고정값이고 라이트·다크 공용이다(흰 글자 대비 5.6:1).
     뱃지 옆에 연번·고유번호가 붙는다.
   - **카드의 구분별 수량 표: 수량 4열은 오른쪽 정렬(자릿수를 세로로 맞춘다), 구분 열만 중앙 정렬**
     (2026-09-12 사용자 요청). 머리글도 해당 열의 정렬을 따른다.
   - ~~사고대비물질 '우선 적용'은 강조색 뱃지~~ **삭제 2026-09-12 사용자 결정**: 카드에 '사고대비물질 · 우선 적용'
     뱃지를 띄우지 않는다. 우선 적용은 순서(사고대비물질이 위)와 인체·생태 유해성 카드의 참고 문구로 드러낸다.
     **공유 텍스트의 첫 줄 라벨은 그대로 `[사고대비물질 · 우선 적용]`이다**(F-003 수용 기준 — 같이 지우지 말 것).
   - **앱 상단 헤더**(2026-09-11 사용자 요청): 왼쪽에 아이콘 심볼 + "FindChem"을 붙여 두고, 오른쪽에 '⋮'(더보기).
     누르면 메뉴가 열리고 일단 톱니바퀴 아이콘 + '설정' 항목 하나가 있다(설정 화면 자체는 F-002, Phase 005).
     2026-09-11 사용자 결정: 목업 확인 없이 이 설명대로 확정.
   - **설정 화면은 영역별 접히는 카드**(2026-09-14 사용자 결정, F-007): data.go.kr API 키 / 연동 데이터 /
     사고대비물질·인체·생태 유해성 정보(F-002) / 웹·앱 정보(F-006 버전). 처음엔 모두 접혀 있다.
   - **CAS 번호는 누를 수 있다**(2026-09-14 사용자 결정, F-007): 누르면 그 카드(웹은 그 행) **바로 아래**에 공공데이터
     조회 결과가 펼쳐진다. 앱·웹이 같은 방식이고 화면 전환은 없다.
4. **다크모드 / 접근성 / 언어**: 시스템 설정을 따라 라이트/다크가 자동으로 바뀐다(Material 3 색 자동 생성).
   화면 언어는 한국어 하나. 시스템 글자 크기 확대에 레이아웃이 깨지지 않게 한다. 스크린리더 라벨 등 별도 접근성
   작업은 하지 않는다(2026-09-11 사용자 결정).
6. **검색 실행 시점**: 글자를 넣을 때마다 즉시 검색한다. 검색 버튼은 없다. 빈 입력이면 결과 영역이 비어 있다
   (2026-09-11 사용자 결정, F-001 수용 기준 테스트는 이 방식을 전제한다).
5. **앱 이름·아이콘**: 앱 이름은 **FindChem**이다. 아이콘은 **C 연결형**으로 확정했다(2026-09-11 사용자 결정).
   물음표 줄기가 삼각플라스크 입구로 이어지고, 물음표의 점이 목 안의 기포가 된다. 배경은 왼쪽 위 남색(#0B1F4F)에서
   오른쪽 아래 파랑(#2E7FE0)으로 가는 그라데이션이다. 확정본: [`docs/design/findchem_icon.svg`](design/findchem_icon.svg)

## 기능 요구사항

### F-001: 물질 검색
- **설명**: 검색창에 물질명(국문·영문)이나 CAS 번호를 넣으면 두 표에서 맞는 항목을 모두 보여준다.
  이름은 부분 일치로 찾고 **띄어쓰기는 무시한다**(2026-09-11 사용자 결정). 화면은 2026-09-11 목업대로 확정했다.
- **수용 기준** (테스트: `test/search/search_test.dart`·`test/ui/search_page_test.dart`, 실제 번들 데이터에 질의):
  - [x] '염화수소'(붙여 씀) → 원문이 '염화 수소'로 띄어 쓴 인체·생태 유해성 연번 306이 결과에 들어간다. 반대로 띄어 써서 넣어도 찾힌다
  - [x] 'GUAZATINE'과 'guazatine' → 둘 다 '구아자틴'과 같은 결과 2건(영문 대소문자 무시 — AI 기본값)
  - [x] '구아자틴' → 2건. 인체·생태 유해성 연번 4(구아자틴 염류)와 연번 5(구아자틴)
  - [x] '13516-27-3' → 1건. 인체·생태 유해성 연번 5. CAS가 여러 개인 항목의 두 번째 이하 CAS로도 찾힌다
  - [x] '50-00-0' → 2건. 사고대비물질 번호 1이 맨 위에 나오고 카드에 '사고대비물질' 뱃지가 붙는다
        ('사고대비물질 · 우선 적용' 뱃지는 2026-09-12 사용자 결정으로 삭제 — 카드에 그 문구가 없어야 한다). 인체·생태 유해성
        연번 510은 그 아래에 나오고, "사고대비물질은 사고대비물질 규정수량을 적용합니다(별표2 일반기준 가)" 참고 문구가 붙는다
  - [x] 결과 머리에 전체 건수와 표별 건수가 나온다. 결과를 상한으로 자르는 경우에는 전체 건수와 '더 있음'을 함께 보여준다
  - [x] CAS 형식으로 입력해 0건이면 "염류·화합물 묶음 항목에 해당할 수 있으니 물질명으로도 검색해 보세요"라는
        안내가 나온다(2026-09-11 사용자 결정 (가))
  - [x] 항목 카드에는 다음이 모두 나온다: 국문명, 영문명, 표 이름 뱃지(표마다 다른 색 — '화면 방향' 3)와 연번,
        고유번호(인체·생태 유해성만), CAS 전부,
        구분별 함량기준·최하위·하위·상위 규정수량(톤). 값은 원문 그대로(`0.2*`, `-`, `70% 초과`)
  - [x] CAS가 없는 항목(77건)에는 '묶음 항목 · CAS 없음' 표시가 붙는다
  - [x] `(삭제)` 항목(인체·생태 유해성 19건)은 결과 맨 아래에 흐리게 나오고 '삭제된 항목 · 규정수량 없음' 표시가
        붙는다(2026-09-11 사용자 결정). '톨루엔' → 사고대비물질 항목이 위에, 인체·생태 유해성 연번 439(삭제)가 맨 아래에 나온다.
        '123-33-1' → 연번 91(삭제) 1건이 나오고, 0건 안내는 뜨지 않는다
- **엣지케이스**: 빈 입력이면 검색하지 않는다(AI 기본값)
- **명세에 없어 AI 기본값으로 정한 것(2026-09-11, 구현 위치 `lib/search/search.dart`·`lib/ui/entry_card.dart`)** — 바꾸려면 여기부터 고친다:
  - 입력이 숫자·하이픈뿐이면 CAS **앞부분 일치**도 본다(입력 즉시 검색이라 타이핑 중에도 좁혀지게). 이름 검색은 항상 같이 한다.
    **하이픈은 무시한다 — '96297'로도 '96-29-7'(연번 1326)이 찾힌다**(2026-09-11 사용자 결정, 테스트 있음). 0건 안내는
    하이픈 뺀 숫자가 5~10자리일 때 띄운다('9999999'는 안내, '9999'는 이름 숫자일 수 있어 안내 없음)
  - 결과 상한은 **100건**. 넘으면 "앞 100건만 표시 · 더 있음"
  - 이름으로 넣어 0건이면 '결과 없음'만 표시(묶음 안내는 CAS 형식일 때만)
  - 참고 문구(와 공유 텍스트의 '우선 적용' 라벨)는 같은 CAS가 **살아있는** 인체·생태 유해성 항목에도 있을 때만 붙는다.
    예: 사고대비물질 톨루엔(108-88-3)의 짝은 (삭제) 연번 439뿐이라 표시하지 않는다
  - 삭제 항목 카드에는 수량 표를 그리지 않는다(값이 전부 `-`). 표별 건수에는 삭제 항목도 센다
  - 2026-09-11 목업은 대화로만 합의했고 저장소에 파일이 없다 — 카드 배치의 정답은 위 수용 기준 문장이다
- **상태**: 완료(2026-09-11)

### F-003: 카드 공유
- **설명**: 결과 카드 오른쪽 위의 공유 아이콘을 누르면 그 카드 내용을 평문으로 공유한다(2026-09-11 사용자 결정, 형식은
  대화에서 예시 3건을 보고 확정). Android는 시스템 공유 창을 연다. 공유 창이 없는 브라우저(PC 웹 등)에서는 클립보드에 복사하고
  "복사했습니다" 알림을 띄운다. 메신저 글꼴은 고정폭이 아니라 표 대신 구분별 한 줄로 쓴다.
- **수용 기준** (테스트: `test/share/share_text_test.dart`·`test/ui/search_page_test.dart`, 실제 번들 데이터):
  - [x] 인체·생태 유해성 연번 510('50-00-0') → 아래 문자열과 한 글자도 다르지 않다. 참고 문구 줄은 참고 문구가 붙는 카드에만 들어간다
        ```
        [인체·생태 유해성] 포르말린; 포름알데히드
        Formalin; Formaldehyde
        연번 510 · 고유번호 97-1-345
        CAS 50-00-0

        규정수량(톤) 최하위 / 하위 / 상위
        · 급성, 함량기준 1%: 0.05 / 2 / 400
        · 만성, 함량기준 0.1%: 0.125 / 5 / 400

        ※ 사고대비물질은 사고대비물질 규정수량을 적용합니다(별표2 일반기준 가)

        FindChem · 「유해화학물질의 규정수량에 관한 규정」 (PDF 2026-07-20 기준)
        ```
  - [x] 사고대비물질 번호 42(염화수소) → 첫 줄이 `[사고대비물질 · 우선 적용] 염화수소`이고, 수량 줄은
        `· 함량기준 10%: 0.02 / 0.8 / 4`와 `· 염화수소 용액: 0.2* / 8* / 40*`이다(구분이 없으면 함량기준만, 값은 원문 그대로)
  - [x] 사고대비물질 번호 46(질산) → 함량기준에 이미 %가 있으면 그대로 둔다: `· 함량기준 70% 초과: 0.025 / 1 / 20`
  - [x] 삭제 항목(연번 91) → 수량 줄 대신 `삭제된 항목 · 규정수량 없음`. CAS 없는 항목(연번 4) → CAS 줄 대신 `묶음 항목 · CAS 없음`
  - [x] 마지막 줄의 날짜는 그 카드가 속한 표 PDF의 생성일이다. 생성일이 없으면 `(PDF … 기준)`을 빼고 고시명까지만 쓴다
  - [x] 검색 결과의 카드마다 공유 아이콘이 하나씩 있고, 누르면 그 카드의 공유 텍스트가 공유 창으로 넘어간다.
        공유 창을 열 수 없으면(예외) 같은 텍스트가 클립보드에 들어가고 "복사했습니다" 알림이 뜬다. 클립보드 쓰기까지
        거부되면 "공유하지 못했습니다" 알림이 뜬다(AI 기본값 2026-09-11 — 앱 내장 브라우저에서 둘 다 실패하는 것을 실측)
  - [x] (사용자 실테스트) Android 실기기에서 공유 아이콘 → 시스템 공유 창 → 카톡 등에 붙은 텍스트가 위 모양 그대로다
        — 2026-09-11 사용자 확인 통과
  - [x] **웹 표 화면(F-004)에는 공유 아이콘이 없다**(2026-09-12 사용자 결정) — 웹에서는 행 복사(TSV)가 대신한다.
        공유 아이콘은 앱 카드에만 있다. 공유 텍스트 형식 자체는 바뀌지 않는다
- **상태**: 완료(2026-09-11, 웹 표에서의 제외는 F-004에서 반영)

### F-002: 원천자료 update
- **설명**: 설정 메뉴의 '원천자료 update'(2026-09-14부터 설정의 '사고대비물질·인체·생태 유해성 정보' 카드 안 — F-007)에서
  인체·생태 유해성 PDF와 사고대비물질 PDF를 올리면, 앱이 파싱해서
  검색에 쓰는 원천자료를 새것으로 바꾼다. 고시가 개정됐을 때 쓴다(2026-09-11 사용자 결정 — 종전의 "앱 재배포로
  갱신" 결정을 대체한다). **update는 기기마다 따로 한다**(2026-09-11 사용자 결정). 서버가 없으므로 한 기기에서
  올린 원천자료가 다른 기기나 웹에 전해지지 않는다.
- **수용 기준** (테스트: `test/data/source_update_test.dart`·`test/data/dataset_loader_test.dart`·`test/ui/settings_page_test.dart`,
  실제 assets/ PDF와 번들 데이터. 저장본 자리만 메모리로 바꾼다):
  - [x] 현재 `assets/`의 PDF 2개를 올리면 1,557건과 100건으로 파싱된다. 결과는 앱에 들어 있는 데이터와 같다
  - [x] **두 PDF를 함께 올려야 한다**(2026-09-11 사용자 결정). 하나만 고르면 적용되지 않고, 무엇이 빠졌는지 알려 준다.
        두 파일을 서로 바꿔 고른 경우(인체·생태 유해성 자리에 사고대비물질 PDF)에도 적용되지 않는다
  - [x] 파싱 검증(표 형식, 연번 연속)이 둘 중 하나라도 실패하면 기존 원천자료를 그대로 두고 실패 사유를 화면에 보여준다
        (연번 연속 판정 자체는 `test/parser/entry_builder_test.dart`가 격자로 건다 — 연번이 끊긴 실제 PDF는 만들 수 없다)
  - [x] 설정 화면에 현재 원천자료 정보가 나온다: 번들 / update 중 무엇인지, PDF 생성일, 표별 건수, 적용한 날짜
  - [x] **'처음 데이터로 되돌리기'**(2026-09-11 사용자 결정)를 누르면 update한 원천자료를 지우고 앱에 들어 있던 데이터로
        돌아간다. 이후 검색 결과와 설정 화면의 원천자료 정보가 번들 기준으로 바뀐다
  - [x] (사용자 실테스트) Android 실기기: 설정 → 두 PDF를 문서 선택기로 골라 적용 → 적용 결과·적용한 날짜가 나오고 검색이 된다.
        앱을 껐다 켜도 update본이 유지된다. 되돌리기 → 번들로. 웹도 같은 흐름(새로고침 뒤 유지)
        — **2026-09-12 사용자 확인 통과**(폰: 건수 1,557/100 일치·고장 상태 2건 / 웹: 같은 흐름. 웹은 첫 빌드가
        플러그인 등록 누락으로 실패해 `flutter clean` 후 재빌드하고 통과 — `work_log/Phase_005.md`)
- **명세에 없어 AI 기본값으로 정한 것(2026-09-12, 구현 위치 `lib/data/`·`lib/ui/settings_page.dart`)** — 바꾸려면 여기부터 고친다:
  - '적용' 버튼은 늘 눌린다 — 빠진 PDF는 누른 뒤 알린다. 적용되면 고른 파일 칸은 비운다
  - 표 형식·연번 검증은 통과했지만 경고(형식이 어긋난 CAS 등)가 있으면 적용하고 경고 건수만 알린다(상세는 로그)
  - 저장본이 깨져 못 읽으면 번들을 쓰고 설정 화면에 사유를 보인다. 저장본을 저절로 지우지는 않고, 이때도 되돌리기
    버튼이 켜져 있어 지울 수 있다
  - 저장본은 앱을 새 버전으로 바꿔 설치해도 남고 계속 우선한다(새 앱의 번들이 더 새것이어도) — 되돌리기로 번들로 간다
  - 되돌리기는 확인 창을 거친다. 저장본이 없으면 버튼이 꺼져 있다. 되돌리면 "앱에 들어 있던 데이터로 돌아갔습니다" 알림
  - 두 PDF가 모두 틀렸으면 두 사유를 함께 보인다
  - 화면 사유에는 상태와 표 검증 사유만 쓰고, 원본 예외 문구(경로 등이 섞일 수 있다)는 로그에만 둔다(보안 3층 ①)
  - 정보 카드에 표별 원본 PDF 파일명도 보인다
  - 날짜 줄: update본은 "적용한 날짜"(적용 시각의 기기 시간대 날짜), 번들은 "데이터 만든 날짜"(번들 추출일)
  - 올리는 파일 크기 상한은 두지 않는다 — 본인 기기에서 본인이 고른 파일이다(보안 점검 결과는 커밋 메시지)
- **상태**: 완료(2026-09-12)

### F-004: 웹 표 화면 + 행 복사(TSV)
- **설명**: **웹에서만** 검색 결과를 표로 그리고(앱은 카드 그대로), 행마다 '복사' 버튼으로 그 물질을
  **탭으로 나뉜 텍스트(TSV)**로 클립보드에 넣는다. 목적은 Excel로 옮겨 붙이는 것이다(2026-09-12 사용자 결정).
  - 근거(실측 2026-09-12, `test/ui/selection_copy_probe_test.dart`): Flutter의 `SelectionArea`는 **여러 위젯에 걸친
    선택을 구분자 없이 이어 붙인다** — 표를 마우스로 긁어 붙이면 Excel 한 칸에 전부 들어간다. 그래서 "보기 좋은 표는
    위젯으로, Excel 이관은 복사 버튼으로" 나눈다.
- **화면(표)**: 열 순서는 **연번 · 화학물질명 · CAS번호 · 고유번호 · 구분 · 함량기준(% 이상) ·
  최하위 규정수량(톤) · 하위 규정수량(톤) · 상위 규정수량(톤) · 복사**.
  - ~~맨 앞 '표' 열(색 뱃지)~~ **삭제 2026-09-12 사용자 요청**: 어느 표인지는 **'구분' 칸이 차 있는지**로 안다
    (인체·생태 유해성은 급성/만성/생태 등이 들어가고, 사고대비물질은 대개 비어 있다). 표 이름 뱃지는 앱 카드에만 남는다
  - 화학물질명 칸은 국문명 아래 영문명(카드와 같은 내용. **TSV에는 국문명만** 넣는다 — 아래)
  - 한 물질의 수량 행이 여러 개면 **앞 칸(연번·물질명·CAS·고유번호·복사)을 세로로 병합**한다
    (2026-09-12 사용자 요청 — 원본 고시 표와 같은 격자 모양. 첫 줄에만 쓰고 나머지를 비우면 별개 행처럼 보인다)
  - 정렬은 카드 표와 같다: 수량 4열·함량기준은 오른쪽, 구분은 중앙, 나머지는 왼쪽
  - 순서·건수 머리·0건 안내·상한 100건은 F-001과 같다(같은 검색 결과를 그린다)
- **복사(TSV)**: 행의 복사 버튼 → **그 물질의 수량 행 수만큼**을 클립보드에 넣는다.
  ~~머리글 1줄을 앞에 붙인다~~ **삭제 2026-09-12 사용자 요청**(여러 건을 이어 붙일 때 머리글이 중간에 끼지 않게).
  - 열은 **연번·화학물질명·CAS번호·고유번호·구분·함량기준(% 이상)·최하위규정수량(톤)·하위규정수량(톤)·
    상위규정수량(톤)** 9개 = 화면 표와 같다. ~~'표'·'영문명' 열~~ **삭제 2026-09-12 사용자 요청**(영문명은 화면에만 둔다)
  - **앞 4칸(연번~고유번호)은 매 줄 반복한다**(2026-09-12 사용자 결정 — Excel에서 정렬·필터가 바로 되게)
  - 값은 원문 그대로. CAS가 여러 개면 한 칸에 `, `로 잇는다. 없으면 빈칸
- **수용 기준** (테스트: `test/share/tsv_text_test.dart`·`test/ui/result_table_test.dart`, 실제 번들 데이터):
  - [x] 웹에서 '50-00-0' → 표에 2행(사고대비물질 번호 1이 위, 인체·생태 유해성 연번 510이 아래). 앱(비웹)에서는 카드가 그대로 나온다
  - [x] '13516-27-3'(구아자틴, 수량 행 2개) → 화면 표는 수량 2줄이고 앞 칸은 **세로로 병합**돼 있다
        (앞 칸 높이가 수량 한 줄보다 크다 — 높이를 재서 확인)
  - [x] 그 행의 복사 버튼 → 클립보드 문자열이 **2줄**(머리글 없음), 각 줄의 탭이 8개(열 9개), 두 줄의 앞 4칸이 서로 같다.
        값 예: `5→구아자틴→13516-27-3, 108173-90-6→97-1-4→급성→1→0.125→5→200`(→는 탭)
  - [x] 복사가 끝나면 "복사했습니다" 알림. 클립보드 쓰기가 거부되면 "복사하지 못했습니다" 알림(조용히 끝나지 않는다)
  - [x] CAS 없는 항목(연번 4) → 화면 CAS 칸은 `묶음 항목 · CAS 없음`, TSV의 CAS 칸은 빈칸
  - [x] 삭제 항목(연번 91) → 행이 흐리게 나오고 수량 칸은 원문 그대로(`-`), TSV도 원문 그대로
  - [x] 웹 표에는 공유 아이콘이 없다(F-003 참조)
  - [x] 폭 375에서도 표가 잘리지 않고 가로로 스크롤된다(표 폭 > 화면 폭이고 가로 스크롤이 실제로 움직인다)
  - [x] (사용자 실테스트) 브라우저에서 복사 버튼 → Excel에 붙이면 **열이 갈려서** 들어간다
        — **통과 2026-09-12**(새 통합 문서에 붙여넣기). 기존 통합 문서에 남아 있던 '텍스트 나누기' 설정이 있으면
        엉뚱하게 잘린다 — 그때는 데이터 → 텍스트 나누기 → 탭 → 마침으로 한 번 초기화한다(Excel 쪽 사정, 앱 문제 아님)
  - [x] (사용자 실테스트) 표가 격자로 보인다: 맨 윗 선·왼쪽 선이 있고, 수량 행이 여럿인 물질도 앞 칸 아래 선이 끊기지 않는다
        — **통과 2026-09-12**
- **명세에 없어 AI 기본값으로 정한 것** — 바꾸려면 여기부터 고친다:
  - 참고 문구("사고대비물질은 …")는 인체·생태 유해성 행의 물질명 칸 아래에 작은 글씨 한 줄. TSV에는 넣지 않는다(열이 어긋난다)
  - 결과 전체를 한 번에 복사하는 버튼은 두지 않는다(2026-09-12 사용자 선택은 '행마다'였다)
- **상태**: 완료(2026-09-12)

### F-005: 자주보는 Chem 목록 (카드 저장)
- **설명**: 결과 카드의 공유 아이콘 옆 **저장 아이콘**을 누르면 그 항목이 '자주보는 Chem 목록'에 들어간다.
  목록은 국문명 가나다 순으로 **접혀** 있고, 펼치면 카드의 정보 전체와 **공유·삭제 버튼**이 나온다.
  목록 머리에는 전체 건수와 **[update] 버튼**이 있다(아래 '[update] 동작').
  **앱에서만** 쓴다(2026-09-12 사용자 결정) — 웹 표 화면에는 저장 아이콘도, 메뉴의 목록 항목도 없다.
  저장은 **그 기기에만** 남는다(서버 없음 — F-002와 같다).
- **저장하는 것 = 카드 내용 전체(스냅샷)** (2026-09-13 사용자 결정 — 종전의 "참조로 저장" 안을 대체한다):
  저장 버튼을 누르면 **그 카드를 그리는 데 필요한 것을 전부** 담는다(항목 전체 + '우선 적용'·참고 문구 여부 +
  그 표의 PDF 기준일). 목록 화면은 **데이터셋을 보지 않고 저장본만으로** 그린다.
  - **왜 참조를 버렸나**: 연번은 키가 아니다 — 개정되면 밀린다. 그렇다고 고유번호+CAS도 키가 못 된다
    (실측 2026-09-13, `scripts/oracle_key_candidates.py`): `uid+cas`가 11조 25건 겹치고
    (`97-1-9`+`7439-92-1` → '납(입자지름≥1mm)'과 '납(입자지름<1mm)'), 별표3은 `uid`가 100건 전부 없고,
    CAS 없는 항목이 77건이다. **참조 방식은 매칭이 실패하면 아무것도 못 보여준다.**
    스냅샷은 매칭이 실패해도 저장할 때 본 내용이 그대로 보인다 — 기능이 매칭 성공에 걸리지 않는다.
  - **개정 반영은 감시가 아니라 버튼으로**(2026-09-13 사용자 결정): 값이 바뀌는 시점은 사용자가 PDF를 올릴 때이고
    사용자가 그 사실을 이미 안다. 그래서 앱이 매번 대조하지 않고, 목록의 **[update] 버튼**을 누를 때 한 번에 맞춘다.
- **[update] 동작**: 저장 항목마다 현재 데이터셋에서 같은 물질을 찾아 **스냅샷을 통째로 새로 쓴다.**
  - 찾는 규칙: 같은 표(`src`) 안에서 **정규화한 이름**(`SearchIndex.normalize` 재사용 — 공백·전각 공백 제거,
    소문자)이 같은 항목. 후보가 둘 이상이면 `uid` → `cas`로 좁힌다. **그래도 하나로 안 좁혀지면 '못 찾음'으로 둔다.**
  - **못 찾은 항목은 덮지 않는다** — 옛 스냅샷을 그대로 두고 표시만 붙인다. 잘못 맞춰 사용자 저장을 망가뜨리는 것보다 낫다.
  - 끝나면 요약을 보여준다: "12건 갱신, 2건은 찾지 못했습니다".
- **정렬** (2026-09-12 사용자 결정): 국문명의 **앞머리(숫자·기호·로마자 접두)를 건너뛰고 첫 한글 음절부터** 비교한다.
  `2-에틸헥산산 납`은 '에'에, `(S)-왈파린`은 '왈'에, `2,5-자이레놀`은 '자'에 들어간다.
  근거: 번들 1,657건 중 **551건(33%)** 의 국문명이 한글로 시작하지 않는다(숫자 379·영문 99·괄호 기호 73, 실측 2026-09-12).
  그대로 정렬하면 이 551건이 전부 '가' 앞에 몰려 찾을 수가 없다. 2차 정렬 키는 표(사고대비물질 먼저) → 연번.
  **건너뛸 접두는 손으로 만든 규칙이므로 전수 검증 테스트를 같은 커밋에 넣는다**(CLAUDE.md 불변 규칙).
- **수용 기준** (테스트: `test/data/favorites_store_test.dart`·`test/ui/favorites_page_test.dart`, 실제 번들 데이터에 질의.
  저장 자리만 메모리로 바꾼다. 한글 `compareTo` 순서의 실측은 `test/data/korean_order_probe_test.dart`):
  - [x] 카드마다 공유 아이콘 **왼쪽**에 저장 아이콘이 하나씩 있다. '구아자틴' 검색 결과 2건의 카드에 저장 아이콘이 2개다
  - [x] 인체·생태 유해성 연번 5(구아자틴)의 저장 아이콘을 누르면 "저장했습니다" 알림이 뜨고, 목록에 '구아자틴' 1건이 있다
  - [x] 저장된 항목의 카드에서는 아이콘이 **채워진 모양**으로 바뀐다. 다시 누르면 "목록에서 뺐습니다" 알림과 함께
        목록에서 빠진다(토글 — 2026-09-12 사용자 결정). 같은 항목이 목록에 2건 생기지 않는다
  - [x] 저장 쓰기가 실패하면 "저장하지 못했습니다" 알림이 뜨고 **기존 목록은 그대로다**(조용히 끝나지 않는다)
  - [x] **정렬**: 연번 88(리누론)·16(2-에틸헥산산 납)·90(말라티온)을 이 순서로 저장하면 목록에는
        **리누론 → 말라티온 → 2-에틸헥산산 납** 순으로 보인다(저장 순서가 아니고, `2-`를 건너뛴 '에'가 ㄹ·ㅁ 뒤다)
  - [x] **정렬**: 연번 16(2-에틸헥산산 납)·325((S)-왈파린)·407(2,5-자이레놀)을 저장하면
        **2-에틸헥산산 납 → (S)-왈파린 → 2,5-자이레놀** 순이다(에 → 왈 → 자)
  - [x] 한글로 시작하는 항목끼리는 가나다 순이다: 연번 88(리누론)·89(헥사클로로시클로헥산)·90(말라티온) →
        **리누론 → 말라티온 → 헥사클로로시클로헥산**
  - [x] 목록의 각 행은 접힌 상태에서 **국문명만** 보인다. 누르면 펼쳐지고, 펼친 내용에는 국문명·영문명·표 이름 뱃지·
        연번·고유번호·CAS 전부·구분별 수량 표가 **F-001 카드와 같게** 나온다(같은 위젯을 재사용한다 — 표현을 두 벌 만들지 않는다)
  - [x] 펼친 안의 공유 버튼을 누르면 **F-003과 한 글자도 다르지 않은** 공유 텍스트가 나간다
        (연번 510을 저장해 펼친 뒤 공유 → F-003 수용 기준의 예시 문자열과 동일)
  - [x] 펼친 안의 삭제 버튼을 누르면 그 항목이 목록에서 빠지고 "삭제했습니다" 알림이 뜬다. 다른 저장 항목은 남고,
        앱을 껐다 켜도 빠진 상태가 유지된다. **확인 창은 띄우지 않는다**(파괴적이지 않다 — 다시 저장하면 복구된다)
  - [x] 목록 머리에 **전체 건수**가 나온다. 저장 개수에 **상한은 두지 않는다**(손으로 하나씩 저장하는 데이터고,
        스냅샷 한 건이 약 300~400바이트라 100건이 40KB 수준이다). 화면 렌더 상한도 두지 않으므로
        '더 있음' 표시가 생길 일이 없다
  - [x] 목록이 0건이면 "저장한 물질이 없습니다" 안내가 나온다(빈 화면이 아니다)
  - [x] 저장 후 앱을 껐다 켜도 목록이 유지된다. 저장 파일이 깨져 못 읽으면 **빈 목록으로 조용히 대체하지 않고**
        사유를 화면에 보이고 로그에 남긴다. 깨진 파일을 자동 삭제하지 않는다(F-002 저장본 선례와 같다)
  - [x] **깨진 저장 파일 복구**(2026-09-13 사용자 결정 — 종전엔 복구 경로가 없었다): 파일을 읽지 못했으면 사유 아래에
        **'깨진 저장 목록 지우기'** 버튼이 있다(정상 파일·0건이면 없다). 누르면 확인 창이 뜨고, 취소하면 파일이 그대로다.
        확인하면 파일을 지우고 "저장 목록 파일을 지웠습니다" 알림 + "저장한 물질이 없습니다" 안내가 나오며, 그 뒤 저장이 다시 된다.
        지우기가 실패하면 "지우지 못했습니다" 알림이 뜨고 파일·사유는 그대로다
  - [x] **목록은 데이터셋 없이 그려진다**: 저장한 뒤 원천자료를 바꿔도(또는 못 읽어도) 목록의 모든 행이
        저장할 때 본 내용 그대로 보인다. 펼친 카드의 수량·CAS·뱃지·참고 문구가 전부 나온다
  - [x] **[update] ①**: 연번 510을 저장한 뒤, 그 항목의 상위 규정수량만 다른 데이터셋으로 바꾸고 [update]를 누르면
        목록의 값이 새 값으로 바뀌고 "1건 갱신, 0건은 찾지 못했습니다"가 나온다
  - [x] **[update] ②**: 저장한 항목이 새 데이터셋에 없으면 **덮지 않는다** — 옛 내용 그대로 남고
        "현재 원천자료에서 찾지 못했습니다" 표시가 붙으며, 요약의 '찾지 못함'에 세어진다
  - [x] **[update] ③**: 이름이 같은 후보가 둘 이상이고 `uid`·`cas`로도 하나로 안 좁혀지면 '못 찾음'으로 둔다
        (조용히 아무거나 고르지 않는다)
  - [x] **[update] ④**: 연번이 밀려도(같은 물질이 다른 연번으로 이동) 이름으로 찾아 갱신된다.
        갱신 뒤 목록에 보이는 연번은 새 연번이다
  - [x] **갱신 알림 줄**: 저장본에 담긴 PDF 기준일과 현재 데이터셋의 기준일이 다르면 목록 머리에
        "원천자료가 바뀐 뒤 아직 갱신하지 않았습니다"와 [update] 버튼이 나온다. 같으면 이 줄이 없다.
        **항목별 대조는 하지 않는다**(날짜 문자열 비교 한 번)
  - [x] **F-002 직후 묻기**: 설정에서 원천자료를 적용하면 그 자리에서 "저장 목록도 갱신할까요?"를 한 번 묻는다.
        '예'면 [update]와 같은 동작이 돌고 같은 요약이 나온다. '아니오'면 아무것도 하지 않고, 목록에는 위 알림 줄이 남는다.
        저장 목록이 0건이면 묻지 않는다
  - [x] **되돌리기와 무관**: 설정의 '처음 데이터로 되돌리기'를 해도 **저장 목록은 지워지지 않는다**
        (저장 파일이 F-002 저장본과 별개다 — SCHEMA.md). 되돌린 뒤 [update]를 누르면 번들 데이터 기준으로 맞춰진다
  - [x] CAS 없는 항목(연번 4)·삭제 항목(연번 91)도 저장할 수 있고, 펼치면 카드와 같게 '묶음 항목 · CAS 없음' /
        '삭제된 항목 · 규정수량 없음'이 붙는다
  - [x] **웹에서는 저장 아이콘이 없고**(F-004 표 화면), '⋮' 메뉴에 '자주보는 Chem 목록'도 나오지 않는다
        (테스트는 `useTable: true`로 확인 — 웹 기본값이 `kIsWeb`인 연결은 VM에서 못 잰다)
  - [x] (사용자 실테스트) Android 실기기에서 저장 → 앱 종료 → 재실행 → 목록 유지 → 펼쳐서 공유 →
        카톡에 붙은 텍스트가 F-003 모양 그대로 → 삭제 — **2026-09-13 사용자 확인 통과**(적용 직후 질문 '아니오' 포함).
        복구 버튼은 그 뒤에 넣었고 자동 테스트로만 건다 — 릴리스 설치본의 앱 전용 저장소에 깨진 파일을 만들 수단이 없다
- **명세에 없어 AI 기본값으로 정한 것** — 바꾸려면 여기부터 고친다:
  - 진입점은 헤더 '⋮' 메뉴의 **첫 항목**('자주보는 Chem 목록', 설정 위). 기존 "'⋮'가 유일한 진입점" 구조를 그대로 지킨다
  - 아이콘은 `star_border` ↔ `star`(채워짐). 이름이 '자주보는'이라 별이 관용에 맞는다
  - 알림 문구 4종: "저장했습니다" / "목록에서 뺐습니다" / "삭제했습니다" / "저장하지 못했습니다"
  - **접힌 행은 국문명만 보인다(사용자 원문). 단 같은 국문명이 2건 이상 저장돼 있을 때만** 그 행들에 표 이름을 덧붙인다
    — 두 표에 같은 이름이 있으면 접힌 행이 똑같아 보여 구분이 불가능하기 때문이다. 이름이 겹치지 않으면 원문대로 이름만 나온다
  - 공유 텍스트 마지막 줄의 PDF 기준일은 **저장본에 담긴 값**을 쓴다(보여주는 값이 스냅샷이므로 기준일도 같아야
    앞뒤가 맞는다. [update]를 누르면 값과 함께 기준일도 새것으로 바뀐다)
  - 설정에 "저장 목록 전체 지우기"는 두지 않는다(YAGNI — 항목별 삭제로 충분)
  - [update]는 **되돌릴 수 없다**(옛 스냅샷을 덮는다). 확인 창은 띄우지 않는다 — 사용자가 개정을 알고 누르는
    버튼이고, 못 찾은 항목은 애초에 덮지 않기 때문이다
  - 국문명에 한글이 하나도 없는 항목은 **현재 데이터에 0건**이다(1,657건 전수 실측 2026-09-12).
    그런 값이 들어오면 국문명 전체를 정렬 키로 쓰고 한글 항목 뒤에 놓는다(방어용 — 지금은 타지 않는 경로다)
  - **구현하며 정한 것(2026-09-13, `lib/data/favorites.dart`·`lib/ui/favorites_page.dart`, spec-checker 대조 후 기록)**:
    - [update] 찾기 규칙의 '이름'은 **국문명(`ko`)** 이다(원문 `name`은 영문명이 붙어 있어 영문 표기만 바뀌어도 못 찾는다)
    - **같은 물질 판정**(저장 아이콘 채움·토글): 표·정규화한 국문명·`uid`·CAS 집합이 모두 같거나, 국문명이 같고
      [update] 찾기 규칙이 그 카드를 가리키면 저장된 것으로 본다 — 개정으로 `uid`·CAS가 바뀐 뒤 아직 [update]를 안 눌렀을 때
      같은 물질이 2건 저장되지 않게(spec-checker 지적). 연번은 보지 않는다
    - 갱신 알림 줄의 기준일 비교는 **항목마다** 그 항목 표의 PDF 생성일 ↔ 현재 원천자료의 같은 표 생성일(문자열).
      항목 내용 대조는 없다. 마지막 [update]에서 **못 찾은 항목은 판정에서 뺀다**(update를 눌렀는데 알림이 남지 않게)
    - '못 찾음' 표시는 파일에 저장하지 않는다 — 앱을 다시 켜면 표시가 사라지고, 그 항목 때문에 알림 줄이 다시 뜬다
      (다시 [update]를 누르면 다시 표시된다)
    - 저장 파일이 깨졌거나 형식 버전이 1이 아니면 **저장·삭제·update를 모두 거부**하고 "저장하지 못했습니다"
      (덮으면 깨진 파일 속 저장이 사라진다). 복구는 사용자가 확인 창을 거쳐 누르는 '깨진 저장 목록 지우기' 버튼뿐이다
      (위 수용 기준 — 저절로 지우지는 않는다). 버튼은 목록 화면의 사유 바로 아래에만 두고 설정에는 두지 않는다
    - [update] 실패(쓰기 실패)도 "저장하지 못했습니다". [update] 버튼은 저장 0건이거나 원천자료를 아직 못 읽었으면 꺼져 있다
    - 펼친 카드의 삭제 버튼은 공유 버튼 **왼쪽**(검색 카드의 저장 아이콘 자리). 툴팁: '저장' / '목록에서 빼기' / '삭제'
    - 번들까지 못 읽는 오류 화면(검색 불가)의 '⋮'에는 목록 항목이 없다
    - 연달아 누르면 앞 알림을 바로 걷고 새 알림을 띄운다(저장 → 빼기가 줄 서지 않게)
- **상태**: 완료(2026-09-13)

### F-006: APK 받기 페이지 + 앱 버전 표시
- **설명**: 친구에게 앱을 줄 때 **링크 하나로 받게** 한다(2026-09-13 사용자 결정). APK 파일은 **GitHub Releases**에 올리고,
  웹 배포 주소 아래 `https://brad0329.github.io/findchem/download/`에 받기 버튼과 설치 안내만 있는 페이지를 둔다.
  받은 사람이 최신판인지 알 수 있게 **설정 화면에 앱 버전**을 보인다(앱·웹 공통).
  - 왜 Releases인가: APK(수십 MB)를 저장소에 커밋하면 버전마다 git 이력에 영구히 쌓이고, 웹 빌드(`build/web`)의 서비스워커
    자원 목록에도 섞인다(실측 2026-09-13 — `flutter_service_worker.js`의 RESOURCES가 `web/`의 모든 파일을 담는다)
- **수용 기준** (테스트: `test/release_test.dart`·`test/ui/settings_page_test.dart`):
  - [x] 설정 화면 맨 아래에 `앱 버전 1.1.0 (빌드 2)`가 나온다. 이 값은 `pubspec.yaml`의 `version: 1.1.0+2`와 같다
        (둘이 어긋나면 테스트가 실패한다 — 버전의 원본은 pubspec이다)
        — **자리 변경 2026-09-14(F-007)**: 설정의 '웹·앱 정보' 카드 안으로 옮기고, 웹에서는 `웹 버전 1.1.0 (빌드 2)`로 쓴다.
        pubspec 대조는 그대로
  - [x] `web/download/index.html`의 받기 링크가 `https://github.com/Brad0329/findchem/releases/latest/download/findchem.apk`
        하나를 가리키고, 최신 릴리스 페이지(`…/releases/latest`)와 웹 앱(`../`) 링크가 있다
  - [x] 페이지에 설치 안내가 있다: 출처를 알 수 없는 앱 허용, Play 프로텍트 경고, 업데이트는 새 APK를 덮어 설치(저장 목록 유지),
        설정 화면의 앱 버전으로 최신판 확인
  - [ ] 배포 주소 `/findchem/download/`가 열린다 — 웹 앱(서비스워커)이 가로채 검색 화면을 띄우지 않는다(배포 후 브라우저로 확인)
  - [ ] (사용자) 릴리스 `v1.1.0`에 `findchem.apk`를 올리면 페이지의 받기 링크가 그 파일을 받는다
  - [ ] (사용자 실테스트) 폰에서 페이지 → 받기 → 설치(기존 설치본 위에 덮어 설치) → 설정의 앱 버전이 `1.1.0 (빌드 2)`이고 저장 목록이 남아 있다
- **명세에 없어 AI 기본값으로 정한 것(2026-09-13)** — 바꾸려면 여기부터 고친다:
  - 올리는 APK는 **통합 APK**(`flutter build apk`, ABI 분할 없음) — 받는 사람 폰의 CPU를 가리지 않게. 파일 이름은 늘 `findchem.apk`
    (최신판 링크가 파일 이름으로 찾는다), 릴리스 태그는 `v` + pubspec 버전의 앞부분(`v1.1.0`)
  - 친구에게 새로 줄 때마다 **pubspec 버전을 올린다**(빌드 번호는 반드시 +1 — Android는 더 낮은 빌드 번호를 덮어 설치하지 못한다)
  - 릴리스 설명에 APK의 SHA-256을 적는다(받은 파일이 올린 파일과 같은지 확인할 수단)
  - 서명 키는 이 PC에만 둔다 — Actions 자동 빌드는 하지 않고, 빌드·업로드는 사람이 한다(절차: `팩_Flutter_Android.md` '친구 배포')
  - 앱의 버전 문자열은 `lib/app_version.dart` 상수이고 pubspec과 대조 테스트로 맞춘다(버전을 읽는 패키지를 추가하지 않는다)
  - 페이지의 "인터넷을 쓰지 않고 권한을 요청하지 않는다" 문장은 실측 근거가 있다(2026-09-13 릴리스 병합 매니페스트의 `uses-permission`은
    androidx 내부 서명 권한 `DYNAMIC_RECEIVER_NOT_EXPORTED_PERMISSION` 하나뿐 — INTERNET 없음). 권한을 쓰는 플러그인을 넣으면 이 문장부터 고친다
    — **F-007(2026-09-14)이 INTERNET 권한을 넣는다 → "인터넷을 쓰지 않습니다" 문장은 F-007 수용 기준대로 바꾼다**
    (INTERNET은 설치 시 자동 부여되는 일반 권한이라 "권한을 요청하지 않는다" 문장은 그대로 참이다)
  - 페이지는 Flutter가 아닌 정적 HTML 한 장이다(스크립트·입력 없음). 최신 버전 번호는 페이지에 적지 않고 릴리스 페이지로 보낸다
    (페이지에 적으면 릴리스마다 고칠 곳이 하나 더 생긴다)
- **상태**: 진행

### F-007: CAS 부가 정보 조회(공공데이터 API 3종) + 설정 카드 구성
- **설명**: 검색 결과의 CAS 번호를 누르면 공공데이터포털 API 3종을 **앱·웹이 직접** 불러 그 카드(웹은 그 행) 바로 아래에
  펼쳐 보인다(2026-09-14 사용자 결정). 인증키는 **사용자가 설정에서 넣는다** — 앱·웹에 키를 넣어 배포하지 않는다.
  **규정수량은 세 API 어디에도 없다** — 규정수량의 원천은 계속 PDF다.
  - 서비스(명세·실측 근거: plan.md 보류 항목 '외부 원천 조사'의 2026-09-14 기록):

    | 화면 이름 | 서비스 | 주소(요지) | 응답 |
    |---|---|---|---|
    | 화학물질 정보 | 한국환경공단_화학물질 정보 조회 15149420 | `B552584/kecoapi/ncissbstn/chemSbstnList`, `searchGubun=2` | JSON |
    | 유독물 GHS 정보 | 한국환경공단_유독물GHS 정보 조회 15149423 | `B552584/kecoapi/ncisghs/ghsList`, `searchGubun=2` | JSON |
    | 안전관리정보(응급 증상) | 화학물질안전원_화학물질안전관리정보 15072442 | `1480802/iciskischem/kischemlist`, `casNo` | **XML 전용** |
  - 이번 범위는 **응답 필드를 받은 그대로** 보이는 것까지다. GHS의 H·P·그림문자는 **코드 그대로**(`H350`) 보인다.
    **코드→국문 문구·그림 변환(화학물질안전원고시 별표 2·3)은 연결 여부를 나중에 사용자에게 다시 묻는다**(2026-09-14 사용자 요청)
    — **그림문자는 연결한다(2026-09-15 사용자 결정)**: 사용자가 준 `assets/유해성 분류.xlsx`(GHS01~09 그림·유해성 분류)를
    `scripts/extract_ghs_pictograms.py`가 그림 9장(`assets/data/ghs/`)과 대응표(`lib/lookup/ghs_pictogram_data.dart`, 생성물)로 만든다.
    목업 2안 중 **안 A(엑셀처럼 가로 칸)** 선택.
    — **H·P 코드도 문구로 보인다(2026-09-15 사용자 결정)**: 사용자가 별표 3 PDF를 옮겨 만든 `assets/유해위험문구(HCODE).xlsx`(82행)·
    `assets/예방조치문구(PCODE).xlsx`(129행)를 `scripts/extract_ghs_phrases.py`가 대응표(`lib/lookup/ghs_phrase_data.dart`, 생성물)로 만든다
- **단계 — 웹 먼저**(2026-09-14 사용자 결정): **오늘은 웹만 구현·배포한다. 앱은 나중에 개발 여부를 사용자에게 확인한 뒤** 한다.
  - 조회 기능(CAS 누르기, 설정 카드 1·2)은 **웹에서만 켠다** — 켜는 분기는 한 곳(`kIsWeb` 기본값). 앱은 지금 동작 그대로다:
    CAS가 눌리지 않고, INTERNET 권한을 넣지 않고, 받기 페이지의 "인터넷을 쓰지 않습니다" 문장도 그대로 둔다
  - 설정의 카드 구성(카드 3·4)은 앱·웹 공통이다(배치만 바뀌고 동작은 같다 — AI 기본값). 설치된 APK는 새로 빌드해 배포하기 전까지 바뀌지 않는다
  - 아래 수용 기준 중 **[앱 단계]** 표시는 앱 착수 때 건다
- **기본 키**(2026-09-14 사용자 결정 — 사용자 1명 단계): 개발자(본인)의 키를 GitHub **Actions Secret `DATA_GO_KR_KEY`**에 두고
  웹 빌드가 기본 키로 넣는다. 사용자가 설정을 열면 키 칸에 `****`와 "기본 키 사용 중"이 보이고 바로 조회된다. 사용자가 자기 키를
  [저장]하면 그 키가 우선하고, [삭제]하면 기본 키로 돌아간다. **사용자가 늘면 로그인과 함께 다시 설계한다.**
  - **기본 키는 공개된다** — 배포된 JS에 들어간다(`****`는 화면만 가린다). 사용자가 위험(호출 한도 소진 → 필요 시 포털에서 재발급)을
    감수하고 결정(2026-09-14, 비기능 4). 저장소·git 이력에는 키가 없다(Secret)
  - 입력칸에는 기본 키 원문을 넣지 않는다 — `****` 흐린 표시만(AI 기본값: 원문을 넣으면 [저장]이 기본 키를 '내 키'로 복사한다)
  - Secret이 등록돼 있지 않으면 기본 키가 없고 지금과 같다(키 입력 안내)
  - **버린 대안**(2026-09-14 검토): ① 저장소에 키 파일 커밋(사용자 첫 제안) — git 이력에 영구히 남고 불변 규칙(비밀정보 커밋 금지) 위반,
    공개 노출 정도는 같다 ② 앱의 [저장]이 GitHub에 파일 쓰기 — 브라우저에 저장소 쓰기 토큰이 있어야 해서, 토큰이 새면 배포된 웹을
    조작할 수 있다(키보다 큰 위험). 토큰도 결국 브라우저마다 넣어야 한다 ③ 중계 서버(Cloudflare Workers 등) — 키가 노출되지 않는 정석이나
    사용자 1명에 과하고 '서버 없음' 결정이 바뀐다 ④ 브라우저 비밀번호 저장 활용 — 같은 브라우저 계정끼리만 옮겨지고 다른 사용자 문제를 못 푼다
- **설정 화면 구성**(2026-09-14 사용자 결정): 영역별 **접히는 카드 4개**, 이 순서. 처음엔 모두 접혀 있다.
  1. **data.go.kr API 키** — 입력창, [키 인증]·[저장]·[삭제]
  2. **연동 데이터** — 조회할 정보 3개의 체크박스. 체크한 것만 조회한다
  3. **사고대비물질·인체·생태 유해성 정보** — 지금 설정 화면의 F-002 내용 전부(현재 원천자료, PDF 선택·적용, 되돌리기)
  4. **웹·앱 정보** — 실행 중인 쪽의 버전(F-006 값)
- **수용 기준** (테스트: 응답은 **2026-09-14 CAS 50-00-0 실호출 원문을 fixture로 고정**해 돈다 — 실호출은 자동 테스트에 넣지 않는다.
  저장 자리는 메모리로 바꾼다):
  - 설정
    - [x] 설정 화면에 카드 4개가 위 순서로 있고 모두 접혀 있다. 카드 3을 펼치면 F-002의 화면 요소가 전부 있고,
          기존 F-002 설정 화면 테스트가 카드를 펼친 뒤 그대로 통과한다
    - [x] 카드 4를 펼치면 앱은 `앱 버전 1.1.0 (빌드 2)`, 웹은 `웹 버전 1.1.0 (빌드 2)`가 나온다(pubspec 대조는 F-006 테스트)
    - [x] 카드 1: 키를 넣고 [저장] → "저장했습니다". 저장소를 다시 읽으면(앱 재실행) 입력창에 같은 키가 있다.
          [삭제] → 입력창이 비고 "삭제했습니다", 다시 읽어도 키가 없다. 쓰기가 실패하면 "저장하지 못했습니다" /
          "삭제하지 못했습니다"이고 기존 저장은 그대로다
    - [x] [키 인증] → **입력창의 키**로 세 서비스를 CAS 50-00-0으로 한 번씩 부르고 **서비스마다 한 줄**을 보인다:
          정상 응답이면 `화학물질 정보 — 확인됨`, 실패면 아래 '실패 문구'. 입력창이 비었으면 호출 0회, "키를 입력하세요"
    - [x] 카드 2: 체크박스 3개(화학물질 정보 / 유독물 GHS 정보 / 안전관리정보(응급 증상)). **처음엔 3개 모두 체크**.
          바꾸면 바로 저장되고 다시 읽어도 유지된다
    - [x] '처음 데이터로 되돌리기'(F-002)는 키·연동 선택을 지우지 않는다(별개 파일 — SCHEMA.md)
  - CAS 조회
    - [x] 앱 검색 카드·웹 표의 CAS 번호가 누를 수 있는 모양이고, CAS가 여러 개면 번호마다 따로 눌린다
          (연번 5 구아자틴: `13516-27-3`, `108173-90-6`). '묶음 항목 · CAS 없음'은 눌리지 않는다.
          자주보는 Chem 목록의 펼친 카드에서는 CAS가 눌리지 않는다. **앱 검색 카드의 CAS 누르기는 [앱 단계]** — 웹 단계에서는
          앱 카드에 링크가 없는 것을 건다
    - [x] 키 저장·3개 체크 상태에서 '50-00-0' → 사고대비물질 카드의 CAS를 누르면 **그 카드 바로 아래**(다음 카드보다 위)에
          결과 영역이 열린다. 받는 동안 '조회 중' 표시, 받은 뒤 섹션 3개가 **화학물질 정보 → 유독물 GHS 정보 → 안전관리정보** 순.
          같은 CAS를 다시 누르면 접힌다. 웹 표에서는 그 행 바로 아래에 같은 내용이 열린다
    - [x] 화학물질 정보 섹션(fixture): 국문명 `포르말린`·영문명 `Formaldehyde`·다른 이름·분자식 `CH2O`·분자량 `30.03`·
          기존화학물질 번호 `KE-17074`, 그리고 분류 **6건**이 각각 분류명·고유번호·함량정보·예외정보·고시일자·고시정보로 나온다.
          빈 값(공백뿐인 고시일자 포함)은 그 줄을 뺀다
    - [x] 유독물 GHS 정보 섹션(fixture): 신호어 `위험`, UN번호 `1198, 2209`, 그림문자 `GHS02, GHS04, GHS05, GHS06, GHS08`,
          M계수 `-`, 분류·고유번호 **6건**(`기존화학물질: V` …), 유해성 분류 **9건**이 각각 분류항목·구분·H코드·P코드로 나온다.
          `^`로 이은 값은 풀어 보이고 코드 자체는 원문 그대로다(`P302+P352`). ~~P코드는 `, `로 이어 한 칸에~~
          **대체 2026-09-15**: H코드·P코드 칸은 아래 'H·P 문구' 기준대로 코드마다 `(코드)문구` 한 줄
    - [x] **그림문자 표**(2026-09-15, 안 A): '그림문자' 줄 바로 아래(UN번호 위)에 응답 코드마다 한 칸, 위에서 **코드 → 그림 → 유해성 분류**.
          fixture 50-00-0 → 5칸이 응답 순서대로 `GHS02`(인화성·물반응성·자연발화성) `GHS04`(고압가스) `GHS05` `GHS06`(급성독성) `GHS08`,
          그림은 `assets/data/ghs/GHS02.png` …, 코드·그림·분류가 같은 열. 기존 '그림문자' 코드 줄은 그대로 둔다.
          대응표에 없는 코드(`GHS99`)는 그 칸에 "그림 없음"(로그에 사유), 그림문자가 없으면 표가 없다.
          코드 줄~분류 줄을 끌어 복사 → 2줄, 칸은 탭, 한 칸의 분류 여러 줄은 `, `로(그림 줄은 글자가 없어 빠진다).
          대응표 9종 = `assets/data/ghs/`의 파일 9개 = 번들 등록(`test/lookup/ghs_pictogram_data_test.dart`)
    - [x] (사용자 실테스트) 웹 배포 주소에서 '50-00-0' → CAS 조회 → 그림문자 5장이 코드·분류와 함께 보인다(그림이 깨지지 않는다)
          — **통과 2026-09-15 사용자 확인**(배포 00933af)
    - [x] **H·P 문구**(2026-09-15 사용자 결정): 유해성 분류 표의 H코드·P코드 칸은 코드마다 **한 줄씩 `(코드)문구`**
          (테스트: `test/lookup/ghs_phrases_test.dart`·`test/lookup/ghs_phrase_data_test.dart`·`test/ui/cas_lookup_test.dart`)
      - 문구표에 있는 코드는 그 문구 — `H300` → `(H300)삼키면 치명적임`, 합성 코드 `H300+H310` → `(H300+H310)삼키거나 피부에 접촉하면 치명적임`
      - 문구표에 없는 코드 → `(H99)문구없음`
      - 문구표에 없는 **합성 코드**는 낱개 코드의 문구를 공백으로 이어 붙이고 끝에 `(문구표 없음)` —
        `P320+P321` → `(P320+P321)<P320 문구> <P321 문구>(문구표 없음)`. 낱개 중 없는 것은 그 자리에 `문구없음`,
        낱개가 **하나도** 없으면 그냥 `(코드)문구없음`
      - fixture 50-00-0의 첫 행(급성독성-경피): H코드 칸 `(H311)<H311 문구>` 1줄, P코드 칸 7줄(`(P280)…`부터 `(P501)…`까지 응답 순서)
      - 선택 복사 때 한 칸의 여러 줄은 **공백 하나로** 이어 한 칸에 둔다(칸 안 줄바꿈은 Excel 칸을 깬다 — 그림문자 표 선례)
      - 대응표 = 엑셀: H 82건·P 129건, 문구에 줄바꿈·앞뒤 공백 없음, 키는 `H123`·`P123`을 `+`로 이은 형태뿐, 코드가 겹치면 생성이 멈춘다
    - [x] (사용자 실테스트) 웹 배포 주소에서 '50-00-0' → CAS 조회 → 유해성 분류 표의 H·P 칸이 `(코드)문구` 줄로 보인다
          — **통과 2026-09-15 사용자 확인**(배포 8a88e02, "화면에 잘 나온다"). 같이 요청한 '유해성 분류 표 복사에 그림문자 표가
          섞이지 않는다'는 사용자 답에 따로 언급이 없어 **자동 테스트(cas_lookup_test 복사 테스트)로만 확인**된 상태다
    - [x] 안전관리정보 섹션(fixture): 일반증상·흡입·피부·안구·경구·기타 **6항목**. 각 항목은 `·` 문장마다 한 줄.
          **문장은 원문 그대로** — 중복 문장(피부의 '반복 노출은 홍반…' 2번)과 `·자료없음`도 지우지 않는다
    - [x] 체크 해제한 서비스는 **호출하지 않고**(가짜 클라이언트 호출 0회) 그 섹션도 없다
    - [x] 키가 없으면 호출 0회, 결과 영역에 "설정 → data.go.kr API 키에서 키를 넣으세요".
          체크가 0개면 호출 0회, "설정 → 연동 데이터에서 조회할 정보를 고르세요"
    - [x] **한 서비스가 실패해도 다른 섹션은 나온다** — 실패한 섹션에만 사유. 결과가 0건이면 그 섹션에 "조회 결과 없음"
    - [x] **조용한 절단 금지**: 응답 `totalCount`가 받은 건수보다 크면 섹션 머리에 "N건 중 M건 표시 · 더 있음"
    - [x] **펼친 결과는 마우스로 끌어 선택하고 Ctrl+C로 복사할 수 있다**(2026-09-14 사용자 요청). 한 항목 안의 여러 문장
          (예: 안전관리정보의 흡입)은 **줄바꿈이 살아서** 복사된다
    - [x] **표·목록을 여러 칸·줄에 걸쳐 복사하면 칸은 탭, 줄은 줄바꿈으로 나뉜다**(2026-09-14 사용자 요청 — Excel에 붙이면 칸·줄이 나뉜다).
          분류 표의 머리글~2행을 끌어 복사 → 3줄, 줄마다 탭 5개(열 6개), **빈 칸도 자리를 지킨다**(`기존화학물질⇥KE-17074⇥⇥⇥20141230⇥…`).
          이름–값 목록은 줄마다 `국문명⇥포르말린`. **한 칸 안에서만 선택하면 선택한 글자만** 복사된다(행 전체가 아니다)
          — Flutter `SelectionArea` 기본은 구분자 없이 이어 붙인다(실측 `test/ui/selection_copy_probe_test.dart`) → 결과 영역은
          구분자를 넣는 선택 컨테이너로 감싼다. ~~칸·항목을 넘나드는 선택은 구분자 없이 이어 붙는다~~ **대체 2026-09-14 사용자 지적**
          (여러 줄이 한 줄로, 표가 Excel에 한 칸으로 들어갔다)
    - [x] (사용자 실테스트) 웹에서 흡입 항목 문장 몇 줄을 끌어 선택 → Ctrl+C → 메모장에 붙이면 줄이 나뉘어 들어간다.
          분류 표 여러 행을 끌어 복사 → Excel에 붙이면 칸·줄이 나뉜다 — **통과 2026-09-14 사용자 확인**(배포 4915201)
          **주의(Excel 쪽 사정, 앱 결함 아님 — 2026-09-15 사용자 확인)**: 셀을 더블클릭한 편집 상태나 수식 입력줄에 붙이면 탭·줄바꿈이
          있어도 **한 셀에 전부** 들어간다. 셀을 한 번만 클릭하고 Ctrl+V로 붙여야 칸·줄이 나뉜다(F-004의 '텍스트 나누기' 잔존 설정과 같은 부류)
  - 실패·보안
    - [x] **실패 문구**(키 인증·조회 공통, 화면에는 상태만 — 원문 응답은 로그에만): HTTP 401 → "키가 올바르지 않습니다" /
          HTTP 403 또는 포털 코드 30 → "이 서비스의 활용신청이 승인되지 않았습니다" / 포털 코드 22 → "오늘 호출 한도를 넘었습니다" /
          접속 실패·시간 초과 → "접속하지 못했습니다" / 그 밖 → "조회하지 못했습니다 (코드 N)".
          HTTP 200이어도 본문이 포털 공통 오류면 그 코드로 판정한다
    - [x] 키는 화면 오류 문구와 로그에 나오지 않는다(로그의 요청 주소·오류 본문에서 가린다 — 가짜 키 문자열로 검사)
    - [x] 포털의 **인코딩 키(`%` 포함)와 디코딩 키**가 같은 요청 주소를 만든다(인코딩 키를 두 번 인코딩하지 않는다 — probe 선례)
  - 기본 키(2026-09-14 추가, 테스트: `test/lookup/api_settings_test.dart`·`test/lookup/chem_api_test.dart`·`test/ui/api_settings_cards_test.dart`)
    - [x] 기본 키가 있고 내 키가 없으면: 카드 1에 "기본 키 사용 중"과 입력칸의 `****` 흐린 표시가 있고, **입력칸 값은 비어 있다**
          (기본 키 원문을 칸에 넣지 않는다). CAS 조회는 기본 키로 부른다
    - [x] 입력칸이 빈 채 [키 인증] → 기본 키로 세 서비스를 확인한다('키를 입력하세요'가 아니다)
    - [x] 내 키를 [저장]하면 조회·인증에 내 키를 쓰고 "기본 키 사용 중"이 사라진다. [삭제]하면 다시 기본 키를 쓴다
    - [x] 기본 키도 내 키도 없으면 지금과 같다("설정 → data.go.kr API 키에서 키를 넣으세요", 호출 0회)
    - [x] 웹 배포 워크플로의 빌드가 Secret `DATA_GO_KR_KEY`를 `--dart-define=DATA_GO_KR_DEFAULT_KEY`로 넣고, 코드가 같은 이름을 읽는다
          (이름이 어긋나면 조용히 기본 키가 사라진다 — 두 파일을 대조하는 테스트로 건다)
    - [x] (사용자) 저장소 Settings → Secrets and variables → Actions에 `DATA_GO_KR_KEY` 등록 — AI는 키를 보지도 넣지도 않는다
          — 2026-09-14 사용자 등록, 배포 88f0449에 들어간 것을 아래 실테스트로 확인
    - [x] (사용자 실테스트) 키를 저장한 적 없는 브라우저(시크릿 창)에서 설정 → "기본 키 사용 중" `****` → [키 인증] 세 줄 확인됨 → CAS 조회 정상
          — **통과 2026-09-14 사용자 확인**(배포 88f0449): "기본 키 사용 중" 표시(Secret이 빌드에 들어갔다), 빈 칸 [키 인증] 세 줄 확인됨, CAS 조회 정상
    - [ ] **[앱 단계]** Android 릴리스 매니페스트에 INTERNET 권한이 있다. 받기 페이지의 "인터넷을 쓰지 않습니다" 문장이
          "CAS 조회(설정에 API 키를 넣었을 때)에만 인터넷을 씁니다"로 바뀐다
  - [ ] **[앱 단계]** (사용자 실테스트) 폰: 설정 → 키 입력 → [키 인증] 세 줄 '확인됨' → [저장] → 앱 재실행 → '50-00-0' → CAS 누름 → 세 섹션.
        **고장 상태**: 비행기 모드에서 CAS 누름 → "접속하지 못했습니다" / 키 한 글자 지우고 [키 인증] → 실패 문구
  - [x] (사용자 실테스트) 웹 배포 주소에서 같은 흐름 — **브라우저 CORS 실측을 겸한다**(2026-09-14: github.io 출처에서 키 없이
        부른 **오류 응답 3건**은 브라우저가 읽었다)
        — **정상 흐름 통과 2026-09-14 사용자 확인**(배포 91ca652): 키 입력 → [키 인증] 세 줄 모두 '확인됨' → [저장] → CAS 조회 정상.
        **브라우저에서 키를 넣은 정상 응답이 읽힌다 — CORS 문제 없음 확정.** 다른 브라우저에서는 키를 다시 넣으라고 나온다(스펙대로 — 위 AI 기본값).
        **고장 상태 2건도 통과 2026-09-14 사용자 확인**(연동 데이터 체크 하나 끄고 조회 → 그 섹션만 빠짐 / 키 한 글자 지우고 [키 인증] → 실패 문구)
- **명세에 없어 AI 기본값으로 정한 것(2026-09-14)** — 바꾸려면 여기부터 고친다:
  - 자주보는 Chem 목록 카드에서는 CAS를 누를 수 없다(사용자 "개발이 편한 쪽" — CAS 누르기를 카드의 선택 콜백으로 두면 목록은 넘기지 않아서 끝난다)
  - 키 입력창은 가리지 않는다(긴 키를 붙여 넣고 눈으로 확인해야 한다. 본인 기기)
  - [삭제]에 확인 창은 없다(포털에서 다시 복사해 넣으면 복구된다 — F-005 삭제 선례)
  - [키 인증] 결과는 화면에만 두고 저장하지 않는다. 인증하지 않은 키도 [저장]된다
  - 한 카드에 결과 영역은 하나다 — 같은 카드의 다른 CAS를 누르면 그 CAS 결과로 바뀐다. 다른 카드는 각자 열 수 있다
  - 응답을 캐시하지 않는다 — 펼칠 때마다 부른다(YAGNI. 개발계정 한도는 chem 10,000건 등)
  - 요청은 `numOfRows=10`, 세 서비스를 동시에 부르고, 시간 초과는 20초
  - 연동 데이터 체크를 바꿔도 이미 열린 결과에는 반영하지 않는다(다음에 누를 때부터)
  - **목업 확인으로 정한 것(2026-09-14 사용자 "목업대로")**: 고시일자는 원문 그대로(`20141230`), 안전관리정보 6항목은 접지 않고
    펼친 채로, P코드는 `, `로 이어 한 칸에. 카드 1 도움말 "공공데이터포털 마이페이지의 일반 인증키를 붙여 넣으세요.",
    카드 2 도움말 "CAS 번호를 누르면 체크한 정보만 조회합니다." + 체크박스 옆 짧은 설명(규제 분류·고유번호 / 신호어·그림문자·H/P 코드 /
    흡입·피부·안구·경구). 결과 영역 머리는 "CAS 50-00-0 공공데이터 조회"
  - **키는 브라우저(웹)·기기(앱)마다 따로 저장된다** — 다른 브라우저·다른 PC·시크릿 창·사이트 데이터 삭제 뒤에는 다시 넣어야 한다
    (서버가 없어 옮겨 줄 곳이 없다. 사용자 실테스트에서 확인 2026-09-14 — 결함 아님, 비기능 4 '그 기기에 저장'의 결과)
  - 입력창이 빈 채 [저장] → 쓰지 않고 "키를 입력하세요". [삭제]는 SCHEMA대로 파일을 지우므로 연동 데이터 체크도 기본값(3개 모두)으로 돌아간다
  - 웹 표에서 결과를 연 행을 화면 밖으로 멀리 스크롤했다 돌아오면(목록 재활용) 다시 부른다 — 열림 상태는 유지된다
  - **H·P 문구(2026-09-15)**: 문구는 엑셀 원문 그대로 둔다 — `(주1)` 같은 주석 표시, `···`, 마침표 중복(`P301+P310`의 `..`)도 고치지 않는다.
    예외 둘: `P250` 문구 안의 줄바꿈은 지운다(`…가하지 마시오\n.` → `…가하지 마시오.`), `P370+P380+P375[+P378]` 행은
    `P370+P380+P375+P378` 코드로 찾는다(`P370+P380+P375`는 자기 행이 따로 있어 그 문구 — 두 행 문구가 다르다). 사용자 예시의 `(P320+321)`은 응답 코드 원문(`P320+P321`)으로 적는다.
    엑셀 '비고' 시트의 (주1)~(주7) 설명은 화면에 넣지 않는다. 표의 칸 폭은 H 문구·P 문구가 넓게 바꾼다.
    사용자가 넣은 문구 원천 PDF는 이름을 `화학물질의 분류 및 표시 등에 관한 규정 [별표 3] 유해위험문구….pdf`로 바꿨다 —
    `[별표 3]`으로 시작하면 규정수량 PDF를 접두사로 찾는 곳 3군데(`scripts/build_data.dart`·`test/parser/fixtures.dart`·
    `scripts/oracle_pdf_cells.py`)가 두 파일을 만나 멈춘다(파이프라인 코드를 고치지 않고 파일 이름으로 피했다)
- **상태**: 진행 — **웹 단계 완료(2026-09-14**, 자동 수용 기준 + 웹 사용자 실테스트 정상 흐름·고장 상태 2건 통과).
  **[앱 단계]는 미착수 — 개발 여부 결정 대기**(2026-09-14 사용자: 사용자들과 논의한 뒤 알려 준다. '안 함'이 아니다).
  **GHS 그림문자 표는 완료 2026-09-15**(사용자 결정, 안 A — 웹 실테스트 통과). **GHS H·P 문구는 완료 2026-09-15**(사용자 결정·엑셀 제공, 웹 실테스트 통과 — 사용자 "종결")

### F-008: 규정수량·최대보유량 판정 도우미 (앱·웹에서 질문 → LLM이 검색 도구를 불러 답)

> **이름 정정 2026-09-16(사용자)**: 종전 이름은 '규정수량 판정 도우미'였다. 1단계에서 **별표 4(최대보유량 산정 방법)
> 전문까지 자산화**했고 spike A1~A3이 그 규칙으로 R값 합산·경계 판정을 통과했으므로, 실제 범위가 이름보다 넓었다.
> 이름을 실제에 맞춘다. **물질별 비중값과 산정 입력폼은 이 기능이 아니라 F-009다**(아래 '울타리').
- **설명**: 검색은 "이 물질의 규정수량은 얼마인가"까지만 답한다. 실제 업무 질문은 한 칸 위다 — "보관시설만 있는 창고에
  톨루엔 0.01톤·황산 0.05톤·염화수소 0.004톤이면 최하위 규정수량 미만인가", "암모니아 0.3톤이면 넘나". 여러 물질 조회 +
  보유량 대조 + 규칙 적용은 조합이라 앱 코드로는 못 한다. **앱·웹 안에서 LLM(Anthropic API)이 우리 검색 함수와 규칙 원문을
  도구로 불러 답한다.** 사용자가 질문을 넣으면 답과 **근거(도구가 돌려준 값)**가 함께 나온다.
  - **출발점 = MCP 서버 spike(2026-09-16, 브랜치 `spike/mcp-server`)**. PC 데스크톱 Claude에 stdio MCP 서버(`scripts/mcp_server.py`
    → `scripts/search_cli.dart`)를 걸고 질문 12개를 던졌다. 확인 ①(조합 판정이 쓸모 있나) ②(도구를 부르나)는 통과(12/12 호출,
    지어낸 사례 0건), ③은 "숫자는 안 뭉개지만 **빈칸을 가정으로 채운다**"(B1·B2 되묻지 않음, D3 규칙 지어냄). 실패 3건의 뿌리는
    **자산화 범위 — 표(별표 2·3)는 자산화했고 표를 지배하는 일반기준(가·나·다)은 안 했다.** 원시 기록 `work_log/spike_mcp.md`,
    해석 `work_log/spike_mcp_review.md`. **MCP 서버는 제품이 아니라 개발 하네스다** — 목적지는 앱·웹이고 MCP는 가장 싼 실험 경로였다.
- **결정(2026-09-16 사용자, spike_mcp_review.md 7절의 열린 질문 5개)**:
  1. **자산화**: 제안 1(별표 2·3 일반기준 원문을 규칙 자산에) **한다**. 제안 2(행마다 적용 조건 문장 + 배타적 선택지)는
     **응답 조립 단계에서만** — 번들 JSON·SCHEMA 불변. 제안 3(커버리지 선언 + 0건/부분 일치만 걸림 구분) **같이 한다**.
     제안 4(공공데이터 API로 묶음 항목 되짚기) **보류**(plan.md 보류 항목).
  2. **모델**: 지금 정하지 않는다. 재실행은 기준선과 같은 **Sonnet 5 고정**(변수 통제 — 자산만 바꾼다). 여전히 실패한 문항만
     Opus 5로 대조하고, 그 결과가 앱 모델 결정이다. legend만 바꾼 상태의 D3 단독 재실행은 건너뛴다(1단계가 흡수).
  3. **F-008로 정식화. 전달 형태 = 앱·웹 내장 tool use**(MCP 없이 검색 함수를 도구 정의에 직접). 서버 프록시(Cloud Functions 등)는
     **안 함**(서버 없음 전제, 정식 운영 시 BYO 키로 갈 예정). **키**: 지금은 테스트 단계(사용자 2명)라 F-007 기본 키와 같은 방식
     (Actions Secret + `--dart-define`, 배포 JS 공개 감수). 정식 운영 시 사용자 각자가 키를 넣는 구조(BYO)로 바꾼다.
  4. **오프라인**: 판정 기능은 **온라인 전용**으로 명시. 통신이 없으면 그 화면에서 알리고 기존 검색으로 안내. 검색·목록은 오프라인 그대로.
     오프라인 규칙 엔진은 만들지 않는다(YAGNI — spike가 확인한 "조합은 LLM이 한다"를 코드로 다시 짜는 일).
  5. **근거 표시는 화면이 강제한다** — 모델 문장이 아니라 **앱이 도구 호출 결과에서 직접** 근거 칸을 그린다. 도구 호출 0건이면
     "도구를 부르지 않은 답"이 그 칸에 보인다(확인 ②를 화면에 올리는 것). 재실행 채점의 인용 칸에서 **출처 표기는 뺀다**(화면이 맡는다).
- **구현 위치 원칙 ★두 번째 구현 금지**: 자산화 3건(규칙 원문·조건 문장·커버리지)은 **Dart 층**(`lib/` 새 도메인 폴더)에 만든다.
  앱이 tool use로 부를 함수가 그 응답을 그대로 쓰기 때문이다. `scripts/search_cli.dart`는 그 함수를 JSON으로 내놓고
  `scripts/mcp_server.py`는 통과만 시킨다(가공·요약 금지 — 지금과 같다). 지금 파이썬 상수인 별표 1 비고·별표 4 원문도 같은 층으로 옮긴다.
  검색 규칙(`lib/search/`)은 손대지 않는다.
- **단계**:
  - **1단계 — 자산화 + MCP 재실행** ✔ **완료 2026-09-16, master 병합**(브랜치 `spike/mcp-server`에서 했다): 아래 수용 기준 '1단계'. 재실행은
    **12개 전부가 아니라 D3·B1·B2·C1만**(기준선 = spike 12문항). A1~A3·C2·C3·D1·D2는 자산 변경과 무관해 안 돌린다.
    한 시나리오 = 한 대화(다른 시나리오 값이 남아 있으면 폐기). 데스크톱 Claude는 설정을 바꾼 뒤 완전히 껐다 켠다.
  - **2단계 — 앱·웹 화면 + LLM 루프**: 아래 수용 기준 '2단계'. 1단계에서 모델이 정해진 뒤 착수. 배포 순서는 **웹 먼저**(F-007 선례 —
    앱은 INTERNET 권한이 필요해 F-007 [앱 단계]와 묶인다. AI 기본값).
- **수용 기준 — 1단계**(자동 테스트: 실제 번들 데이터·`assets/` PDF에 직접. 구현은 `lib/assist/`, 테스트는 `test/assist/`):
  - [x] 규칙 조회에 **별표 2 일반기준·별표 3 일반기준** 주제가 있고, 원문이 `assets/`의 해당 PDF 문단과 **줄 단위로 같다**(손으로 옮긴
        텍스트 → 대조 테스트 필수). 별표 1 비고·별표 4도 같은 층·같은 대조. 알 수 없는 주제는 오류(조용히 전체로 대체하지 않는다).
        **선행 확인 결과(2026-09-16)**: 일반기준은 별표 2·3 PDF **1쪽, 표가 시작되기 전 '1. 일반기준' 절**에 있다 — 표 밖이라 현재
        파서가 건드리지 않는 것이 맞다. 별표 2는 가·나·다 셋, 별표 3은 가 하나뿐이다.
        **대조 방식(착수 시 결정)**: pdfplumber 정답지 방식(기존 `oracle_pdf_cells.py`와 같은 구조) —
        `scripts/oracle_rule_text.py` → `test/fixtures/rule_text.json` → `test/assist/rules_test.dart`가 공백 무시 줄 대조.
        `--selftest`에서 파이썬 상수와 함께 걷어냈다(상수가 Dart로 갔으므로 파이썬에서 대조할 대상이 없다).
        수식 글꼴이라 줄 대조에서 뺀 별표 4의 16줄은 `excluded`에 남기고, 그 내용(R 공식·Qn·QLLTn)은 테스트가 따로 확인한다
  - [x] **조건 문장**: 항목의 수량 행마다 "언제 이 행을 적용하는가" 문장이 붙는다. 값은 그 행의 원문 필드(구분·함량기준)와 일반기준
        원문에서만 만든다(지어내지 않는다). '암모니아' → 사고대비물질 항목의 행 2개(기체 / 암모니아 용액)에 서로 다른 조건 문장이 있고,
        함량기준이 빈 행에는 일반기준 나·다의 빈칸 해석이 문장에 들어간다. **전수 테스트**: 모든 수량 행에 문장이 생기고 빈 문장이 0건
        **실측 정정(2026-09-16)**: 스펙화 때 적은 "1,657건 모든 수량 행"의 1,657은 **항목 수**였다 — 수량 행은 **2,458개**.
        "함량기준이 빈 행 258개"도 별표2만 센 값이다 — 별표2 258(저확산 256 + 용액 2) + 별표3 3(용액 행) = **261개**.
        별표3의 빈칸은 일반기준이 다루지 않아(원문에서 함량기준 칸이 항목당 하나) 그 사실만 문장에 적는다
  - [x] **배타적 선택지**: 한 물질(같은 항목)에 수량 행이 둘 이상이면 응답에 "성상·구분을 확인해야 한다"는 표시와 행별 선택지가 있다.
        '암모니아'·'염화수소'는 있고, 행이 하나뿐인 '톨루엔'(사고대비물질)에는 없다.
        **착수 시 덧붙임**: 행이 둘 이상이어도 **성상(배타)과 유해성 구분(병존)은 다르다** — 유해성 구분 행이 여럿이면
        "하나를 고르라"가 아니라 별표 1 비고 제2호(가장 작은 수량)를 가리킨다. 고시가 정하지 않은 것(저확산 행 대 유해성 구분 행)은
        가리키기만 하고 결론짓지 않는다
  - [x] **커버리지 선언**: 모든 응답에 "이 도구는 이 고시 별표 2·3만 덮는다. 여기 없으면 이 고시의 규정수량 대상이 아니다" 문장이 있다.
        결과 상태는 셋 중 하나로 명시된다 — `exact`(이름·CAS 일치 있음) / `partial_only`(정확 일치 없이 부분 일치만) / `none`(0건).
        '아세톤' → `partial_only` 2건(아세톤 시아노히드린 등)과 "부분 일치는 별개 물질일 수 있다" 문장 / '67-64-1' → `none` + 묶음 안내(hint) /
        '황산' → `exact`. **착수 시 덧붙임**: 선언에 묶음 항목 단서를 한 문장 붙였다 — "여기 없으면 대상이 아니다"가 CAS 없는
        묶음 항목(염류·"~와 그 화합물")까지 잘라 버리지 않도록
  - [x] 조용한 절단 금지 유지: 상한(100건)을 넘으면 `truncated`와 전체 건수가 응답에 있다(spike CLI와 같다)
        **2단계에서 상한을 20으로 낮춘다(2026-09-16 사용자 결정 — 비용 실측 근거는 2단계 수용 기준)**.
        `truncated`·`total`을 함께 주는 구조는 그대로라 조용한 절단 금지는 유지된다. 검색 화면(F-001)의 상한은 100 그대로 —
        `SearchIndex(ds, {cap = 100})`가 생성자 인자라 판정 도구만 다른 값으로 만든다(`lib/search/`는 손대지 않는다)
  - [x] `scripts/mcp_server.py --selftest`가 통과하고, 서버는 CLI 출력을 가공하지 않는다(파이썬 규칙 상수는 사라지고 Dart 층이 원본).
        `get_rule`도 CLI(`--rule <주제>`) 통과로 바뀌어 서버에 규칙 텍스트가 한 줄도 남지 않는다
  - [x] (사용자 실테스트 — 데스크톱 Claude, **Sonnet 5**, 문항마다 새 대화) 채점은 호출·값·인용 세 칸, **인용에서 출처 표기 제외**:
        **결과 2026-09-16 — D3 O / B2 O / C1 O / B1 △(실패) → 실패 문항 B1을 Opus 5로 던져 통과 → 앱 모델 = Opus 5.**
        상세·회차별 기록은 `work_log/spike_mcp.md` '1단계 재실행 기록'. B1은 자산 구멍(함량기준 미만 갈래)을 메워
        Sonnet 5로 한 번 더 돌렸는데도 미달이라 **등급 차이로 판정**했다(B2는 자산으로 메워졌다 — 등급 차이가 아니었다)
        - **D3** "메틸알코올은 어느 표 기준으로 보나요?" → 사고대비물질(별표 3) 우선 적용을 **일반기준 가 원문**을 근거로 말하고,
          함량기준 차이(85% / 10%)를 말하며, "더 엄격한 쪽 적용" 류의 고시에 없는 규칙이 **없다**
        - **B1** "톨루엔 3톤 있는데 규정수량 넘나요?" → 함량·시설유형을 되묻거나, 되묻지 않으면 **최하위 0.05톤과 함량기준 85%**를 포함한
          갈래를 제시한다(85% 미만 혼합물이면 판정이 뒤집힌다는 점이 답에 있다)
        - **B2** "암모니아 0.3톤이면 규정수량 넘나요?" → 기체(0.05, 넘음)와 **암모니아 용액(0.5\*, 넘지 않음)** 두 갈래를 모두 제시하거나
          성상을 되묻는다. 용액 행을 감추면 실패. 별표(\*)가 살아 있다
        - **C1** "아세톤 5톤 보관 중인데 규정수량이 얼마죠?" → "이 고시 별표 2·3에 개별 항목이 없다"를 명시하고 부분 일치 2건을 별개 물질로 가른다
        - 넷 중 실패한 문항만 **Opus 5**로 같은 자산에서 다시 던진다 → 통과하면 "그 문항에는 Opus 5가 필요"가 결론이고 앱 모델은 Opus 5,
          전부 Sonnet 5로 통과하면 앱 모델은 Sonnet 5. 결과는 `work_log/spike_mcp.md` 기록 표에 모델 이름과 함께 적는다
- **수용 기준 — 2단계**(자동 테스트: LLM 응답은 **고정 응답(fixture)**으로 돈다 — 실호출은 자동 테스트에 넣지 않는다.
  검색 도구는 실제 번들 데이터):
  - [ ] 헤더 '⋮' 메뉴에 '규정수량·최대보유량 판정(AI)' 항목이 있고(자주보는 Chem 목록·설정과 같은 진입 구조), 누르면 질문 입력창·답·근거 칸이
        있는 화면이 열린다. 앱·웹 공통. **새 탭·새 네비게이션은 만들지 않는다** — '⋮'가 유일한 진입점인 화면 전환 구조를 그대로 따른다
        (CLAUDE.md '작업 트랙' ②. 구조를 바꾸려면 일반 트랙이라 사용자에게 먼저 묻는다).
        **메뉴 순서(2026-09-16 사용자 지시) = 자주보는 Chem 목록 → 규정수량·최대보유량 판정(AI) → 설정.**
        자주보는 Chem 목록은 앱 전용이라 웹에서는 판정 → 설정 둘만 보인다(F-005와 같다).
        **판정 항목은 앱·웹 둘 다 켠다(2026-09-16 사용자 지시 "app도 인터넷 되도록")** — 같은 날 릴리스
        `AndroidManifest.xml`에 INTERNET 권한을 넣었다(종전에는 debug·profile에만 있어 릴리스 APK에서 호출이 막혔다).
        F-007 CAS 조회는 별개로 웹만 그대로다
  - [ ] LLM에 주는 도구는 둘 — 검색(1단계 Dart 함수 그대로)과 규칙 조회. 도구 응답 JSON은 CLI가 내는 것과 **같은 함수**로 만든다(대조 테스트 불필요 —
        구현이 하나다)
  - [ ] **웹 CORS 헤더(2026-09-16 실측)**: 요청에 `anthropic-dangerous-direct-browser-access: true`가 있다.
        **실측 근거** — `https://example.com` 오리진에서 무효 키로 `POST /v1/messages`를 두 번 불렀다:
        헤더 없으면 `TypeError: Failed to fetch`(CORS 차단, 응답이 오지 않는다) / 헤더가 있으면 **401 `authentication_error`가 도달**한다.
        빠뜨리면 아래 '오프라인·실패'의 접속 실패로 **오인**되므로 요청 헤더를 대조 테스트로 건다(F-007 이름 대조 선례)
  - [ ] **검색 도구 응답 상한 = 20건**(2026-09-16 사용자 결정). 상한을 넘으면 `truncated`·`total`이 응답에 있다(1단계와 같다).
        **비용 실측 근거**(`count_tokens` 실호출, 2026-09-16): CAS 1건 질의 응답 = 2,477 입력 토큰 / 넓은 질의(`acid`, 100건 절단) = **55,211 토큰**.
        대화는 왕복마다 통째로 재전송되므로 넓은 질의 하나가 끼면 Opus 5 기준 한 질문에 $1.4 내외가 된다(좁은 질의만이면 $0.15 내외).
        자동 테스트: 21건 이상 걸리는 질의에서 `returned`가 20이고 `total`이 실제 건수다
  - [ ] **프롬프트 캐싱**(2026-09-16 사용자 결정, 같은 근거): 왕복마다 재전송되는 프리픽스에 `cache_control`을 달아 누적 재전송 비용을 낮춘다.
        자동 테스트: 요청 본문에 `cache_control` 블록이 있다(fixture로 요청을 가로채 확인)
  - [ ] **스트리밍(SSE)으로 받는다**(2026-09-16 사용자 결정 — AI 권장은 비스트리밍이었으나 사용자가 스트리밍을 골랐다).
        답 텍스트가 오는 대로 화면에 붙는다. **새 의존성은 필요 없다** — `http` 1.6.0의 `BrowserClient`가 XHR이 아니라
        `fetch` + `ReadableStream` 리더를 쓰고(`browser_client.dart:83,174`) Android는 `IOClient`가 원래 스트리밍이라,
        `Client().send(Request)` 한 경로로 앱·웹이 같이 돈다. 자동 테스트: fixture SSE를 조각으로 흘려보내면 화면 텍스트가 조각마다 늘어난다
  - [ ] 시스템 프롬프트에 spike의 설계 조건이 들어 있다: 도구가 준 것 이상을 말하지 말 것 / 한 물질에 행이 둘 이상이면 되물을 것 /
        함량기준을 답에 넣을 것 / 규정수량은 기간 처리량이 아니라 어느 순간의 최대 체류량. 프롬프트·도구 설명의 원본은 코드 한 곳
  - [ ] **근거 칸**: 답과 별도로, 이번 답을 만들며 호출된 도구 결과를 앱이 그린다 — 물질별 표 이름(화면 용어)·연번·수량 문자열(별표 \* 그대로)·
        함량기준·고시명, 쓰인 규칙 주제. fixture(도구 호출 3건 + 규칙 1건)에서 물질 3줄 + 규칙 1줄이 나온다
  - [ ] **도구 호출 0건**으로 답이 오면 근거 칸에 "도구를 부르지 않은 답 — 값을 검색으로 확인하세요"가 보인다
  - [ ] 모델 ID는 상수 한 곳이고 **기본값은 1단계 결정값인 Opus 5(`claude-opus-5`)**다. 키는 `--dart-define`으로 들어오며
        **키가 없으면** 화면에 "이 빌드에는 AI 키가 없습니다" 안내와 함께 질문을 보내지 않는다(호출 0회)
  - [ ] **사용자 키 입력(2026-09-16 사용자 결정 — `hanjadic` 계열 앱의 설정 기능을 참고하라는 지시)**: 설정 화면에 Anthropic 키 칸이 있고,
        **사용자 키가 있으면 우선, 없으면 빌드 기본 키**를 쓴다(F-007 `serviceKey`와 같은 규칙). [저장]·[삭제]·[키 확인].
        `hanjadic`에서 베낀 것 둘 — ① 입력칸 **마스킹 + 눈 아이콘**으로 보기/가리기(F-007 칸은 평문이다)
        ② **[키 확인]은 `max_tokens: 1` 최소 호출**로 인증만 본다(요금 최소화). 실패는 인증(401/403)과 접속 실패를 갈라 문구를 다르게 준다.
        **베끼지 않은 것**: 평문 파일 저장 계층 전체(그쪽은 앱 전용·`dart:io`라 웹에서 안 돈다 — 우리는 기존 `UpdateStore` 한 벌을 쓴다)와
        설정 잠금(4자리, 소스에 기본값이 박혀 있다). 키 파일 읽기 실패 때 **예외 원문을 로그에 찍지 않는 것**은 우리 F-007이 이미 그렇다
  - [ ] **모델 선택 설정(2026-09-16 사용자 결정)**: 설정 화면(F-007의 `lib/ui/settings_page.dart`)에서 **Opus 5 / Sonnet 5**를 고를 수 있다.
        기본값 **Opus 5**. 고른 값은 그 기기에 남고(앱 재시작·웹 새로고침 뒤에도 유지) 판정 화면이 그 값으로 호출한다.
        저장된 값이 없거나 **모르는 값이면 기본값으로 떨어지고 그 사실을 로그에 남긴다**(조용히 넘어가지 않는다).
        근거: 1단계에서 **같은 자산으로 두 모델의 답이 갈렸다**(B1 — Sonnet 5 미달, Opus 5 통과). 비용 차이가 있어
        (Sonnet 5 $2/$10 vs Opus 5 $5/$25 per MTok) 쓰는 사람이 고르게 한다.
        자동 테스트: 설정에서 Sonnet 5를 고르면 **요청 본문의 모델 ID가 `claude-sonnet-5`로 바뀐다**(fixture로 요청을 가로채 확인).
        선택지는 이 둘뿐이다 — 목록을 일반화하지 않는다(YAGNI)
  - [ ] **오프라인·실패**: 접속 실패·시간 초과 → "오프라인이거나 접속하지 못했습니다. 검색은 계속 쓸 수 있습니다" / HTTP 401 → "AI 키가
        올바르지 않습니다" / 429 → "요청 한도를 넘었습니다" / 그 밖 → "답을 받지 못했습니다 (코드 N)". 화면에는 상태만, 원문은 로그에만.
        키는 로그·화면에 나오지 않는다(F-007 규칙과 같다)
  - [ ] 답을 받는 동안 '답 작성 중' 표시. 도구 호출이 이어지는 동안(여러 물질 조회) 화면이 멈추지 않는다
  - [x] **개발 중 실호출 스모크**(2026-09-16 사용자 승인) — 로컬 웹 빌드(`--dart-define-from-file`)를 브라우저에서 띄워 B2를 넣었다.
        **fixture가 못 잡은 결함 1건을 잡았다**: 두 모델 모두 thinking이 켜져 있어 응답에 `thinking` 블록이 오는데,
        기본값 `display: omitted`는 본문이 빈 문자열이고 그 빈 블록을 다음 왕복에 되돌리면
        `each thinking block must contain thinking`(400)이 난다. → 요청에 `display: summarized`를 넣고
        `thinking_delta`·`signature_delta`를 모아 **본문·서명 그대로** 되돌린다. fixture에도 thinking 블록을 넣어 회귀를 걸었다.
        **UI 결함 3건도 실호출에서만 드러났다**(위 'AI 기본값'의 마크다운·Enter·빈 줄)
  - [ ] (사용자 실테스트) 웹 배포 주소에서 **A1**(3물질 합산, R 0.8 → 미만)·**B2**·**C1**·**D3**을 넣어 답과 근거 칸을 확인한다.
        **고장 상태**: 비행기 모드(또는 네트워크 차단)에서 질문 → 오프라인 안내, 검색은 된다
  - ~~**배포 선행 조건 — Anthropic 콘솔 지출 한도**: 한도 확인 전에는 배포하지 않는다~~
        **철회 2026-09-16(사용자 결정)**: "이럴 필요 없다, 그냥 바로 배포해". 선행 조건을 두지 않고 배포한다.
        **위험은 그대로 남는다** — 배포 JS의 키는 공개되고 Anthropic API는 종량제라 제3자가 긁어 쓰면 곧장 청구된다
        (F-007의 data.go.kr 키는 무료·쿼터라 최악이 '쿼터 소진'이었다 — 위험의 종류가 다르다).
        지출 한도는 **권고 사항으로 남긴다**(사용자가 원할 때 콘솔에서 건다). 사용자 키 입력이 설정에 있으므로
        Actions Secret이 없어도 배포는 되고, 그때는 각자 키를 넣어 쓴다
  - [ ] (사용자 실테스트) 폰에서 같은 흐름. ~~INTERNET 권한은 F-007 [앱 단계]와 함께~~
        **정정 2026-09-16(사용자 지시)**: 판정은 앱에서도 켠다 — INTERNET 권한을 릴리스 매니페스트에 넣었고
        F-007 [앱 단계]와 분리했다. 실기기 설치·실행은 사용자가 한다
- **명세에 없어 AI 기본값으로 정한 것(2026-09-16)** — 바꾸려면 여기부터 고친다:
  - Dart 도메인 폴더 이름은 `lib/assist/`(판정 도우미). 화면은 `lib/ui/assist_page.dart`
  - 대화는 한 화면에서 이어진다(A1→A2→A3처럼 맥락을 이어받는 질문이 있다). 화면을 나가면 대화는 사라지고 저장하지 않는다(YAGNI)
  - ~~답 텍스트는 그대로 보이고 마크다운 렌더링은 하지 않는다(의존성 추가 없음)~~ **정정 2026-09-16(사용자 결정)**:
    **마크다운을 렌더링한다** — 실호출에서 모델이 `**굵게**`와 표를 많이 써서 날것으로 보이면 읽기 나빴다.
    의존성 `flutter_markdown_plus`(폐기된 `flutter_markdown`의 후속, 검증 퍼블리셔, 표 지원)를 추가했다.
    근거 칸은 F-001 카드 위젯을 재사용하지 않고 한 줄 텍스트
  - **Enter로 보낸다, Shift+Enter로 줄바꿈**(2026-09-16 실호출에서 발견) — 입력칸이 여러 줄이라 `onSubmitted`가 불리지 않는다.
    입력칸을 감싸는 `Focus`로는 안 되고 TextField가 쓰는 `FocusNode.onKeyEvent`여야 한다(브라우저 실확인 완료)
  - **예시 질문 버튼 4개를 입력창 아래에 둔다**(2026-09-16 사용자 요청 — 종전엔 힌트 문구로 예시 하나만 있었다).
    누르면 **바로 보낸다**(입력창에 넣어 두고 기다리지 않는다). 넷은 서로 다른 함정을 건드린다 —
    성상에 따라 갈림(암모니아) / 함량기준(톨루엔) / 여러 물질 합산(별표 4 제3호 다목) / 어느 표를 적용하는가(별표 2 일반기준 가).
    앱·웹 같은 화면이다. 원본은 `assistExamples`(`lib/ui/assist_page.dart`) 한 곳
  - **근거는 대화 단위로 들고 간다**(2026-09-16 사용자 실테스트에서 발견): 이번 답에서 도구를 부르지 않았어도
    **앞선 질문에서 부른 값이 있으면** 그것을 보이고 칸 제목으로 그 사실을 밝힌다. 명세가 A1→A2→A3처럼 이어 묻는 것을
    전제하므로, 이어지는 답의 근거를 지우면 멀쩡한 답이 근거 없는 답으로 보인다.
    대화 첫 질문부터 도구 없이 답한 **진짜** 경우에만 "도구를 부르지 않은 답"이 뜬다
  - **도구 호출 앞뒤 문장 사이에 빈 줄을 넣는다**(2026-09-16 실호출에서 발견) — 그대로 이으면 "…확인하겠습니다.## 결론"처럼 붙는다
  - 시간 초과 60초(도구 호출이 여러 번 이어진다), 도구 호출 왕복 상한 10회(넘으면 "답을 받지 못했습니다"와 함께 지금까지의 근거 칸).
    **스트리밍이라 60초는 '한 왕복이 끝날 때까지'가 아니라 '조각이 하나도 안 올 때'를 재는 값이다**(글자가 흐르는 중이면 끊지 않는다)
  - 웹 배포 워크플로의 Secret 이름은 `ANTHROPIC_API_KEY`, `--dart-define=ANTHROPIC_DEFAULT_KEY`(F-007과 같은 대조 테스트로 이름 어긋남을 잡는다)
  - **로컬 개발 실행의 키 주입은 `--dart-define-from-file=<gitignore된 JSON>`** — 셸 명령에 키를 쓰지 않아 명령 기록·트랜스크립트에 남지 않는다
    (CLAUDE.md 불변 규칙 '비밀정보는 채팅에 쓰지 않는다')
  - **모델 파라미터는 기본값 그대로** — Opus 5·Sonnet 5 모두 thinking이 adaptive로 켜져 있고(조합 판정에 유리) `effort` 기본값은 `high`다.
    `budget_tokens`는 두 모델에서 400이라 쓰지 않는다. 비용이 문제로 드러나면 `effort`를 먼저 내린다
  - **모델 선택 설정(2026-09-16)**: 저장은 F-007 인증키와 **같은 저장소**(앱=지원 디렉토리 파일, 웹=localStorage)에 키 하나를 더 두는 방식.
    파일을 새로 늘리지 않는다. 화면 표기는 제품명 그대로 'Opus 5' / 'Sonnet 5'이고, 어느 쪽이 비싸고 정확한지는
    한 줄 설명으로 적는다("Opus 5 — 더 정확하고 더 비쌈(기본값)"). 저장 형식 변경이므로 착수 시 SCHEMA.md 게이트를 먼저 탄다
- **울타리 — 이 자산(별표 1~4)으로 답할 수 있는 것과 없는 것**(2026-09-16 정리):
  - **답한다**: 물질별 규정수량(최하위·하위·상위)과 함량기준 / 어느 표·어느 행을 적용하는가(성상·용액·저확산·사고대비물질 우선) /
    유해성 구분이 둘 이상일 때 가장 작은 수량(별표 1 비고 2) / **최대보유량을 무엇으로 산정하는가**(설계용량·비중, 기상물질,
    혼합물은 전체 총량) / **보관시설만 운영하는 사업장의 합산 판정**(R = ΣQn/QLLTn < 1) / 저확산 400톤 물질의 특례
  - **못 한다 ① 물질별 비중값이 없다.** 별표 4는 "비중을 고려하여 산정한다"고 **요구만** 하고 값은 주지 않는다. 번들 JSON에도
    F-007 공공데이터 API에도 없다 → "20㎥ 탱크가 몇 톤인가"는 사용자가 비중을 불러줘야 나온다. **F-009**
  - **못 한다 ② 산정 입력폼이 없다.** 설계용량·시설유형·함량·용기 수는 대화로 받을 수는 있어도 구조화된 입력이 아니다. **F-009**
  - **못 한다 ③ "넘으면 무슨 의무가 생기나"는 범위 밖.** 별표 1~4는 *수량이 얼마고 어떻게 세느냐*까지다. 영업허가·장외영향평가·
    취급시설 기준은 법령 본문·시행규칙 소관이라 자산에 없다. 넣으려면 그 고시를 새로 자산화해야 한다(번호 없음, 아이디어 단계)
  - **못 한다 ④ "왜 이 수량인가"**(유해성 그룹 → 수량 도출). 별표 1의 산정표를 싣지 않았다 — 결론값이 별표 2·3에 이미 있어서다
  - **못 한다 ⑤ 묶음 항목 판정**(CAS 없는 77건, "납과 그 화합물" 류) — 원천 PDF에 그 정보가 없다(F-008 제안 4, plan.md 보류)
- **상태**: 진행 — **1단계 완료(2026-09-16)**: 자산화 + 자동 수용 기준 6개 + 사용자 실테스트(D3·B1·B2·C1) 통과,
  **앱 모델 = Opus 5 확정**. master 병합 완료. **2단계(앱·웹 화면 + LLM 루프) 미착수** — 모델 선택 설정 포함

### F-009: 최대보유량 산정 보조 (비중 데이터 + 산정 입력폼)
- **설명**: F-008은 최대보유량을 **어떻게 산정하는지**(별표 4 규칙)까지 답하지만, **실제 숫자를 내지는 못한다.**
  막힌 곳이 둘이다 — ① 물질별 **비중값**이 어디에도 없다(별표 4는 "비중을 고려하여 산정한다"고 요구만 한다.
  번들 JSON에도 F-007 공공데이터 API에도 확인된 바 없다) ② 설계용량·시설유형·함량·용기 수를 받는 **구조화된 입력**이 없다.
  이 둘을 채우면 "저장탱크 20㎥ × 2기, 톨루엔 90% → 몇 톤 → 규정수량 대비 어디"까지 화면이 답한다.
- **왜 F-008과 나눴나(2026-09-16 사용자 결정)**: F-008 2단계는 화면 + LLM 루프만으로 배포할 수 있다.
  비중 데이터 확보가 안 되면 2단계 전체가 막히므로 **번호를 나눠 배포를 분리**했다. F-008의 이름은 이미
  '규정수량·최대보유량 판정'으로 넓혔다 — 규칙을 설명하는 데까지가 F-008, 숫자를 내는 것이 F-009다.
- **착수 전에 먼저 할 것 — 조사**: 물질별 비중값의 출처가 있는가.
  후보 ⑴ F-007이 이미 부르는 공공데이터 API 3종의 미사용 필드 ⑵ 다른 공공 데이터셋 ⑶ 사용자가 직접 입력.
  **⑴·⑵는 `researcher`로 실측 확인**(CLAUDE.md '문서에 적힌 API·필드를 기억으로 쓰지 말 것').
  ⑶으로만 가능하면 기능의 값이 크게 떨어지므로 **그 결과를 보고 착수 여부를 사용자가 정한다.**
  새 외부 호출이 생기면 **보안 3층 ②** 대상이고, 비중 데이터를 번들에 넣으면 **일반 트랙**(SCHEMA.md 게이트)이다.
- **수용 기준**: 미작성 — 위 조사 결과를 받은 뒤 스펙화한다(구현 전에 반드시 채운다)
- **상태**: **미착수**(2026-09-16 번호 발급). F-008 2단계 완료 후 검토

## 비기능 요구사항

> **게이트: 첫 외부 입력 진입점(공개 엔드포인트·폼·업로드·웹훅)을 만들기 전에 아래 1·2가 채워져 있어야
> 한다.** 비어 있으면 그 자리에서 사용자에게 묻는다(CLAUDE.md '보안 3층' ②). F-002의 PDF 업로드가 이 게이트에 걸린다.

1. **인증 방식**: **없음**(2026-09-12 사용자 결정). 앱을 가진 사람 누구나 설정에서 원천자료 update·되돌리기를 한다.
   근거: update는 그 기기에만 적용되고(서버 없음) '처음 데이터로 되돌리기'로 복구된다
2. **공개 범위**: 본인과 사내 소수가 쓴다. APK는 직접 설치하거나, **링크를 아는 사람이 GitHub Releases에서 받는다**
   (F-006, 2026-09-13 사용자 결정 — 받기 페이지는 공개 URL이라 누구나 받을 수 있다. 저장소·데이터가 이미 공개라 새로 드러나는 정보는 없다).
   웹은 **GitHub Pages**에 올린다 — `https://brad0329.github.io/findchem/` (2026-09-12 사용자 결정).
   **URL을 아는 사람은 누구나 열 수 있다**(Pages 접근 제한은 유료 플랜). 저장소가 이미 공개이고
   데이터는 환경부 고시라 새로 공개되는 정보가 없다는 근거로 수용했다. 로그인·인증은 두지 않는다(위 1번)
3. **성능·기타**: PDF 파싱 중 웹 화면이 몇 초(Chrome 약 2.6초, Phase 003 실측) 멈추는 것을 허용한다 — '파싱 중' 표시를
   먼저 띄운 뒤 파싱한다. 웹 워커로 분리하지 않는다(2026-09-12 사용자 결정)
4. **외부 호출·비밀정보**(F-007, 2026-09-14 사용자 결정): 앱·웹이 data.go.kr을 **직접** 부른다(서버는 여전히 없다).
   인증키는 앱·웹에 넣어 배포하지 않고 **사용자가 설정에서 넣어 그 기기에 평문으로 저장**한다(Android 앱 지원 디렉토리 파일,
   웹 localStorage). 근거: 받기 페이지로 누구나 APK를 받으므로 키를 넣어 배포할 수 없다. 무료 공공데이터 키라 유출 피해는
   호출 한도 소진 수준이다. 키는 요청 주소의 `serviceKey`(HTTPS)로만 나가고 화면 오류 문구·로그에는 쓰지 않는다.
   **주의**: 웹 localStorage는 출처 `brad0329.github.io` 단위라 **같은 계정의 다른 GitHub Pages 저장소 페이지도 읽을 수 있다** —
   이 계정에 다른 공개 페이지를 올리게 되면 다시 본다. 보안 3층 ②: 이 기능의 커밋은 변경 범위 점검 대상이다(비밀정보·새 외부 호출)
   **기본 키 공개 감수**(2026-09-14 사용자 결정, F-007 '기본 키'): 웹 빌드에 개발자 키(Actions Secret)를 기본 키로 넣으므로 그 키는
   배포된 JS에서 누구나 볼 수 있다. 사용자 1명 단계라 편의를 택했고, 피해는 호출 한도 소진(포털 재발급으로 복구)이다.
   **다시 볼 때**: 사용자가 늘거나 로그인을 넣을 때, 또는 한도 소진·오남용 흔적이 보일 때. 저장소·git 이력에는 키가 없다
5. **LLM 호출(F-008, 2026-09-16 사용자 결정)**: 앱·웹이 Anthropic API를 **직접** 부른다(서버 프록시 없음). 키는 테스트 단계(사용자 2명)라
   F-007 기본 키와 같은 방식(Actions Secret → `--dart-define`, 배포 JS 공개 감수). 공공데이터 키와 달리 **유출되면 남이 과금을 쓴다** —
   그래서 저장소·커밋에는 절대 넣지 않고(공개 저장소는 비밀 스캐너가 키를 폐기한다), 사용자가 늘면 **BYO 키(각자 설정에 입력)**로 바꾼다.
   판정 기능은 **온라인 전용**(오프라인이면 안내, 검색은 그대로). 보안 3층 ②: 이 기능의 커밋은 변경 범위 점검 대상이다(비밀정보·새 외부 호출)

## 하지 않는 것 (Out of Scope)
- 서버 DB. ~~외부 API 연동 — 이 프로젝트가 끝난 뒤 다시 검토한다~~ **대체 2026-09-14 사용자 결정: F-007에서 공공데이터 API 3종을
  앱·웹이 직접 부른다**(키는 사용자가 설정에서 입력 — 비기능 4). 규정수량을 API로 받는 것은 여전히 하지 않는다(세 API에 없다)
- 스토어 등록
- CAS가 어느 묶음 항목("납과 그 화합물" 등)에 속하는지 판정. 원천 PDF에 그 정보가 없다
  (F-008 제안 4 — 공공데이터 API로 되짚는 안은 **보류**, plan.md 보류 항목)
- **LLM 호출 중계 서버(Cloud Functions·Workers 등) — 안 함**(2026-09-16 사용자 결정, F-008): 서버 없음 전제를 깨고, 정식 운영 시
  BYO 키로 갈 예정이라 필요가 없다. 다시 볼 때: 불특정 사용자에게 운영자 키로 서비스해야 할 때
- **오프라인 규정수량 판정(앱 내 규칙 엔진) — 안 함**(2026-09-16, F-008): 조합 판정은 LLM이 하고, 오프라인에서는 검색만 된다

## 미결 질문
- (없음 — 웹 호스팅 위치는 2026-09-12 GitHub Pages로 확정, 위 비기능 2)
